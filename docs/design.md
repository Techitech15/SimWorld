# SimWorld 設計方針

> RimWorld風2Dブラウザ入植地シミュレーション。本ドキュメントは実装レベルの設計を定める。
> **実装に追随して更新する**。いま何がどう動くかを書く場所であり、実装と食い違ったらこちらを直す。

| | |
| --- | --- |
| **このファイル** | 仕様。いま何がどう動くか |
| [design-notes.md](design-notes.md) | 追加検討時の設計思想。なぜそうしたか、何を測ったか、案から何を変えたか |
| [design-phase2.5-animals.md](design-phase2.5-animals.md) | **フェーズ2.5** 生物レイヤーの設計案（提案時のまま。現況は notes 側） |
| [design-phase5-trade.md](design-phase5-trade.md) | **フェーズ5** 交易とファンタジー層の設計案（実装済み。実装との差分は同ファイル末尾の実装メモ） |
| [design-phase6-space.md](design-phase6-space.md) | **フェーズ6** マップ拡張と画面の再配置の設計案（A-1〜A-3・B-1〜B-3 実装済み。実装との差分は同ファイル末尾の実装メモ） |
| [design-phase7-time.md](design-phase7-time.md) | **フェーズ7** 昼夜の表現・なめらかな移動の設計案（実装済み） |
| [design-phase8-equipment.md](design-phase8-equipment.md) | **フェーズ8** 服と武器の設計案（E-1〜E-4・E-6 実装済み。E-5 防寒着のみ保留） |
| [design-phase9-language.md](design-phase9-language.md) | **フェーズ9** 日本語表示と言語切り替えの設計案（**実装済み**。提案時のまま。現況は 11 章） |
| [design-phase10-ores.md](design-phase10-ores.md) | **フェーズ10** 鉱石と家具・建築メニュー階層化の設計案（**実装済み**。提案時のまま。現況は 11 章） |
| [design-phase11-worldmap.md](design-phase11-worldmap.md) | **フェーズ11** ワールドマップ・バイオーム・部族の設計案（**実装済み**。提案時のまま。現況は 11 章） |
| [design-phase12-research.md](design-phase12-research.md) | **フェーズ12** 研究ツリーと職業の設計案（**実装済み**。提案時のまま。現況は 11 章） |
| [design-phase13-presentation.md](design-phase13-presentation.md) | **フェーズ13** 画面の整えと音の設計案（実装済み） |
| [design-next.md](design-next.md) | 未実装のものの検討。実装したらこちらの 11 章と notes へ移す |
| `README.md` | 動かし方・操作・テスト一覧 |

原案（MVP 着手時点の版）は Git 履歴に残っている（`docs/design.md` を追加したコミット `534c005`）。
本文を原案のまま保つ必要はないので、以降は現状に合わせて書き換える。ただし
**「なぜそうしたか」はここには書かない** —— それは notes 側の役割で、仕様と理由は寿命が違うため。
## 目次
1. 概要と設計原則
2. 技術スタックと採用理由
3. アーキテクチャ
4. データモデル
5. 時間モデル
6. ジョブシステム詳細
7. 経路探索
8. セーブ／ロード
9. MVP機能リスト
10. 実装順序
11. フェーズ2以降のロードマップ
12. ドット絵アセット仕様
---
## 1. 概要と設計原則
SimWorldは、ファンタジー世界観の入植地シミュレーションである。プレイヤーは3人の入植者から始め、伐採・採掘・農作・建築・運搬の生産チェーンを回して生存を続けさせる。マナ結晶を燃料とする魔力インフラが自動化・防衛の土台になる（11章フェーズ2、段階A〜C 実装済み）。
守るべき設計原則は3つ。
1. **データとロジックを分離する。** エンティティ（入植者・建築・アイテム・ジョブ）は振る舞いを持たないプレーンなデータとし、ロジックはすべて外部関数に置く。理由: これを守らないとセーブがJSON化だけで完結しなくなり、後から矯正するコストが破滅的に高い。
2. **シミュレーションとUIの再描画を分離する。** ゲーム状態ストアを唯一の真実源とし、PixiJSとReactはどちらもそれをsubscribeするだけで、互いのループに干渉しない。理由: Reactの再レンダリングがゲームループのfpsを揺らす設計は、入植者が増えた瞬間に破綻する。
3. **土台が回ってから積む。** 最適化パズル（生産チェーン）が気持ちよく回ることを最優先する。理由: 土台が壊れている状態で機能を積み増しても、後で土台からやり直すことになる。MVP を終えた現在も同じ規則で運用しており、追加は必ず段階に割って、段階ごとに「動いたと言える条件」をテストにしてから実装する（10章）。
4. **主張する前に測る。** バランスも性能も「こうなるはず」ではなく、年単位のヘッドレス実行か実ビルドで測ってから書く。測った値と食い違ったら、直すのは主張の側。実際に何度もそうなっており、記録は [design-notes.md](design-notes.md) にある。
## 2. 技術スタックと採用理由
| 採用 | 理由（却下した代替とその理由） |
| --- | --- |
| **Vite** | 開発サーバの起動・HMRが速く、TypeScriptをネイティブに扱える。Webpackは同等のことをするのに設定コストが高いため却下。 |
| **TypeScript** | データモデルを型で強制できる。純粋データ設計（4章）はJSでは実行時まで壊れに気づけないため必須。 |
| **PixiJS** | WebGL経由のスプライトバッチ描画が得意で、60×60タイル＋複数エンティティの2D描画に十分な性能を持つ。生のCanvas API直書きはスプライト数が増えると即座に重くなるため却下。Phaser等の統合ゲームエンジンは、シーン管理・入力・UIまで自前で抱え込む設計思想がReactとの責務分離（3章）と衝突するため却下。 |
| **React** | RimWorld系はUIがゲームの半分を占める（入植者タブ・仕事優先度表・資源一覧・健康画面）。宣言的UIと豊富なコンポーネント資産で複雑な表形式UIを素早く組める。Vue/Svelteも技術的には十分だが、この種の管理UIでの採用実績・知見の蓄積はReactが厚いため優先。 |
| **状態ストア（zustand想定）** | サブスクライブ粒度を選べる軽量ストアで、PixiJS側もReact側も同じストアを購読できる。Reduxはボイラープレートがこの規模のMVPに対して過剰なため却下。自前Pub/Subの完全自作は車輪の再発明でバグの温床になるため却下。 |
## 3. アーキテクチャ
4層に分離し、依存の向きを一方向に固定する。
```
[Simulation層] --tick書き込み--> [State Store (GameState)] <--購読のみ-- [Rendering層 (PixiJS)]
                                        ^
                                        |--アクション書き込み（優先度変更・建築指定など）
                                        |
                                [UI層 (React)] --購読のみ（表示用）--> (Storeから読む)
```
- **State Store**: `GameState`（4章）を保持する唯一の真実源。PixiJSもReactもここ以外からゲームの状態を読まない。ストアへの書き込みは (1) Simulation層のtick処理、(2) UI層が呼び出す「アクション関数」（例: `setJobPriority(colonistId, jobType, priority)`、`placeBuildingBlueprint(...)`）の2経路に限定する。UI層のアクション関数はSimulation層を経由せずStoreへ直接書き込むが、その結果（例: 新しいブループリント）は次のtickでJobGenerator（6章）が拾って処理する。理由: 書き込み経路をこの2つに絞らないと、PixiJS側とReact側で状態が二重管理されズレる。
- **Simulation層**: 固定tickでGameStateを次の状態に進める純粋関数群（5章・6章・7章）。DOMにもPixiにも依存しない。理由: ロジックを描画から切り離すことで、将来ヘッドレステスト（画面なしでシミュレーションだけ検証）が可能になる。
- **Rendering層（PixiJS）**: `requestAnimationFrame`ループで動き、ストアをsubscribeしてスプライトの位置・テクスチャを同期する。ストアへは一切書き込まない（クリック等の入力はUI層/入力ハンドラを経由してアクション関数を呼ぶ）。
- **UI層（React）**: `useSyncExternalStore`相当のフックでストアの必要な部分だけをselector購読する（例: 入植者パネルは選択中の1人分のみ購読）。理由: GameState全体をpropsで渡すと、ticks更新のたびに無関係なコンポーネントまで再レンダリングされる。
**Reactの再レンダリングとゲームループの分離方針**: Simulation層のtickは描画フレームレートと非同期に進む（5章）。PixiJSはRAFで毎フレーム描画するが、Reactはストアの変更通知があったコンポーネントのみ、かつ変更のあったselectorの値が実際に変わったときのみ再レンダリングする。ゲームループがReactのcommitフェーズを待つことは一切ない。
## 4. データモデル
全型はプレーンなデータであり、メソッドを持たない。クラス・`Date`・`Map`・`Set` は使わない。相互参照はオブジェクト参照ではなくID（`string`）で行う。理由: ID参照ならJSON化しても壊れないが、オブジェクト参照は循環構造を生み `JSON.stringify` できない。この原則が「`GameState` はそのままセーブになる」（8章）を成立させている。

実体は `src/core/types.ts`。

```typescript
type TileId = string;       // `${x},${y}`
type ColonistId = string;
type BuildingId = string;
type ItemId = string;
type JobId = string;
type ZoneId = string;
type AnimalId = string;
type Vector2 = { x: number; y: number };

// crystal は岩盤の中のマナ結晶。建築ではなく地形なので、採掘は既存の mine ジョブがそのまま扱う
// ironVein はその前例をなぞる2つ目の鉱脈（フェーズ10）。産出は VEIN_YIELD の表が決める
type TerrainType = 'grass' | 'forest' | 'stone' | 'crystal' | 'ironVein';
type Designation = 'chop' | 'mine' | 'deconstruct';

interface Tile {
  id: TileId;
  x: number;
  y: number;
  terrain: TerrainType;
  walkable: boolean;        // 岩・結晶は採掘前 false。地形変更で更新される
  buildingId: BuildingId | null;
  itemIds: ItemId[];        // 地面に落ちているアイテム
  designation: Designation | null;
  forage: number;           // 草地の可食量 0..1。放牧で減り1日で戻る
}

type ResourceType = 'wood' | 'stone' | 'food' | 'manaCrystal' | 'iron';
interface Item {
  id: ItemId;
  type: ResourceType;
  quantity: number;
  position: Vector2;        // 貯蔵ゾーン内でも実座標を持つ
  reservedByJobId: JobId | null;
}

interface ColonistNeeds {
  hunger: number;           // 0(満腹)〜100(餓死寸前)
  sleep: number;            // 0(覚醒)〜100(限界)
  recreation: number;       // 0(足りている)〜100(うんざり)。2日で満杯
}

// ニーズ由来の振る舞いはジョブシステムの外側に置く（プレイヤーが優先度を付ける仕事ではないため）
type ColonistActivity =
  | { kind: 'none' }
  | { kind: 'moving'; targetTileId: TileId }
  | { kind: 'eating'; itemId: ItemId | null; ticksRemaining: number }
  | { kind: 'sleeping'; bedId: BuildingId | null }
  | { kind: 'fleeing'; fromAnimalId: AnimalId; untilTick: number }
  | { kind: 'relaxing'; hearthId: BuildingId | null; untilTick: number }
  // メンタルブレイク3種。いずれも仕事を止める。違うのは植民地が何を見せられるか
  | { kind: 'brooding'; untilTick: number }
  | { kind: 'wandering'; untilTick: number }
  | { kind: 'binge'; untilTick: number; eaten: number };

interface Colonist {
  id: ColonistId;
  name: string;
  color: number;                    // 共通スプライトへの tint（12章）
  position: Vector2;
  path: Vector2[] | null;           // 経路キャッシュ（7章）
  pathTargetTileId: TileId | null;
  needs: ColonistNeeds;
  currentJobId: JobId | null;
  health: number;                   // 0..100。部位なし・病気なし・治療ジョブなし
  carrying: { type: ResourceType; quantity: number } | null;
  activity: ColonistActivity;
  workPriorities: Record<JobType, number>;   // 0=無効, 1(最高)..3(最低)
  skills: Record<SkillName, number>;         // 累積経験値。レベルは導出する
  traits: TraitName[];                       // 生涯不変。すべて既存の数値への乗数
}

type BuildingType =
  | 'wall' | 'stoneWall' | 'floor' | 'stoneFloor' | 'door' | 'bed'
  | 'farmPlot' | 'berryBush' | 'frostbloom' | 'storageZoneMarker'
  | 'manaFurnace' | 'manaConduit' | 'manaLamp' | 'manaExtractor' | 'manaTurret'
  | 'hearth' | 'tradingPost'
  // 家具5種（フェーズ10）。効果値は種類ごとの定数表にあり、Building には載せない
  | 'table' | 'stool' | 'dresser' | 'armchair' | 'statue';

interface Building {
  id: BuildingId;
  type: BuildingType;
  tileId: TileId;
  isBlueprint: boolean;
  hpCurrent: number;
  hpMax: number;
  requiredResources: { type: ResourceType; quantity: number }[];
  buildProgress: number;    // 0..1
  growth: number;           // 畑・ベリー: 0=裸地, 1=収穫可
  sown: boolean;            // 畑のみ
  manaFuel: number;         // 魔導炉に残っている燃焼 tick 数
  manaProgress: number;     // 自動採掘機が岩を削った進捗。-1 は「掘る岩が無い」
}

type JobType = 'chop' | 'mine' | 'farm' | 'build' | 'haul' | 'hunt' | 'handle' | 'deconstruct' | 'repair';
type JobState = 'pending' | 'reserved' | 'active' | 'completed' | 'failed' | 'cancelled';

interface Job {
  id: JobId;
  type: JobType;            // 実行内容
  workType: JobType;        // どの仕事列で優先度を引くか（資材運搬は「建築」の仕事）
  priority: number;
  targetTileId: TileId | null;
  targetEntityId: string | null;
  destinationId: string | null;      // 運搬先（貯蔵タイル・ブループリント・魔導炉）
  payloadType: ResourceType | null;
  workProgress: number;
  state: JobState;
  reservedBy: ColonistId | null;
  createdAtTick: number;
  retryCount: number;
  cooldownUntilTick: number | null;
}

interface Zone {
  id: ZoneId;
  type: 'storage' | 'pasture';
  tileIds: TileId[];
  accepts: ResourceType[];  // 貯蔵ゾーンの受け入れ資源。牧場は food のみ
}

interface Animal {
  id: AnimalId;
  species: AnimalSpecies;   // deer | boar | rabbit | chicken | goat | wolf | crystalElk | rockeater
  name: string;
  position: Vector2;
  path: Vector2[] | null;
  pathExpiresAtTick: number | null;  // 動物の経路は PathIndex に載せず、失効させる（7章）
  hunger: number;
  health: number;
  ageTicks: number;
  tame: boolean;
  pastureZoneId: ZoneId | null;
  designation: 'hunt' | 'tame' | 'slaughter' | null;
  activity: AnimalActivity;
  produceAtTick: number | null;
  gestationUntilTick: number | null;
}

interface GameState {
  tick: number;
  speed: 0 | 1 | 3 | 10;
  tiles: Record<TileId, Tile>;
  colonists: Record<ColonistId, Colonist>;
  buildings: Record<BuildingId, Building>;
  items: Record<ItemId, Item>;
  jobs: Record<JobId, Job>;
  zones: Record<ZoneId, Zone>;
  animals: Record<AnimalId, Animal>;
  raiders: Record<RaiderId, Raider>;        // 襲撃中だけ中身がある
  traders: Record<TraderId, Trader>;        // 来訪中だけ中身がある
  reservations: Record<string, Reservation>;
  forestCapacity: number;   // この地図が支える森の量。再生の不動点
  worldSeed: number;        // 災厄の暦を世界ごとに変えるため
  scenario: ScenarioName;   // 生成時だけでなく毎日効く規則がある
  nextIds: Record<string, number>;
  relationships: Record<string, number>;   // 入植者ペアごとの affinity（キーはID2つをソートして結合）
  deaths: { colonistId: ColonistId; name: string; tick: number }[];  // 直近の死者。喪の対象
  log: LogEntry[];
}
```

補足: 経路（`path`）・予約（`reservations`）もセーブ対象に含める。理由: ロード直後に全員が経路探索をやり直すのは無駄で、予約が消えると2人が同じ木に向かう事故（6章）が再発するため。

**保存しないもの（導出する）**: 経路インデックス、連結領域ラベル、魔力ネットワーク（`ManaNetwork` / `ManaGrid`）、気分と思考。いずれも `SimContext` かその場の計算で、セーブには入らない。理由: 導出できる値を保存すると「実態とズレた値」という不具合の種類が増える。

## 5. 時間モデル
- **tick長**: 1 tick = 200ms（5 tick/秒）を基準周波数とする。理由: A*やジョブ候補フィルタを毎フレーム（60fps）ではなくtick単位で走らせることで、経路探索コスト（7章）を安全な頻度に抑えられる。
- **1日の長さ**: 1日 = 3,000 tick。1倍速では 3,000 tick ÷ 5 tick/秒 = 600秒（10分）で1日が経過する。
- **速度倍率**: 停止(0) / 1倍 / 3倍 / 10倍。いずれも **tick長は変えず処理回数だけを変える**。理由: tick長を可変にすると経路キャッシュや予約のタイムアウト計算（6章）がずれる。
  - 停止（0倍）: tickカウンタを進めない。GameStateへの書き込みは一切発生しない。
  - 10倍は1日を60秒にする。季節・災厄・熟練はすべて「日」の尺度で起きるので、これが無いとゲームの大半が「遊ぶもの」ではなく「読むもの」になる。シミュレーションは1 tick あたり約0.6ms（1コアの約3%）で、律速は最初から描画側だった。
- **1年**: 1季5日・4季で20日（`src/core/season.ts`）。作物の成長・草の再生・繁殖・気分が季節で変わる。
- **pause時の扱い**: Simulation層のtickループを完全停止する。PixiJSのRAF描画ループ自体は止めない（カメラ操作のため）が、GameStateが変化しないのでスプライトは静止する。Reactの操作（優先度変更や建築指定）はpause中も受け付け、次にtickが進んだ瞬間に反映される。
**ニーズは空腹・睡眠・娯楽の3つ**。いずれもしきい値マーカーを持たず直線的に増加し、しきい値到達で自動的に食事・睡眠・休息へ遷移する。娯楽は 2 日で満杯になり、炉端に座って解消する ——**他人によって満たされる唯一のニーズ**で、仲間が近くにいると回復が1.5倍になる。空腹と睡眠には常に譲る（飢えたまま火に当たる者はいない）。

快適・美しさは**ニーズにしていない**。床・寝床・灯・冬は既に気分の思考として載っており、バーを2本増やしても同じことを二重に言うだけだから。

気分（mood）と思考（thought）は実装済みだが**ニーズではない**。導出値であり、空腹・睡眠・体力・寝床・倉・床・灯・季節から毎回計算する。詳細は下の11章と [design-notes.md](design-notes.md)。

### tick パイプライン

順序は好みではなく、いくつかは入れ替えると壊れる。

```
beginTick → rebuildRegions（地形が変わっていれば）
  → growCrops → regrowForest → runIncidents → runArrivals
  → runNeeds ★ → runMoveOrders → runAnimals ★ → runFleeing → healColonists
  → runMana → runJobGenerator → runAssignment → runExecution → cleanupJobs
```

★1 `runNeeds` はジョブ生成より前でなければならない。中断された仕事が同じ tick のうちにキューへ戻っていないと、生成器と候補フィルタが古い状態を見る。
★2 `runAnimals` は割り当てより前でなければならない。同じ tick に捕食者に追われた入植者へ仕事が割り当てられてはいけない。
## 6. ジョブシステム詳細
素朴な実装（各Colonistが毎tick「一番近い未処理の木」を探して直接向かう）は、2人が同じ木に向かって片方が無駄足になる、運搬したアイテムをまた別のColonistが運び出す無限ループ、といった事故を起こす。原因は「候補を選ぶ」と「実際に確保する」の間に競合防止がないことなので、**予約（reservation）を最初からジョブのライフサイクルに組み込む**。
### RimWorld準拠の簡略化
RimWorld wiki「Work」によると、実際の優先度システムは次の通り。
- 標準モードでは「割り当て済み/未割り当て」のみで、Work Menu上で左にある仕事ほど優先。マニュアルモードでは各仕事タイプに1〜4の優先度を手動設定できる。
- **「同一優先度の仕事は、次の優先度に移る前に全て終わらせる」**。距離やマップ上の位置は一切考慮しない（真横の木より地図の反対側の木を先に処理することもある）。
本作ではこれを次のように簡略化する。
- 優先度は**1〜3の3段階**（0=無効）。RimWorldの1〜4より単純化。
- 仕事は9種（`chop` / `mine` / `farm` / `build` / `haul` / `hunt` / `handle` / `deconstruct` / `repair`）だが、**優先度の列は7つ**。`deconstruct` と `repair` は建設の列で扱う——壁を壊すのも直すのも、建てたのと同じ技能だから。この「実行内容（`type`）」と「どの列で選ばれるか（`workType`）」の分離が `Job.workType`。ブループリントへの資材運搬は「建築」の仕事として扱う（そうしないと運搬を最低優先度にした植民地で建物が永久に完成しない）。
- 同一優先度内の候補選択は**距離最短のジョブを優先**する（RimWorldの「効率無視」方式は採用しない）。理由: MVPは3人・60×60という小規模マップであり、非効率な移動が目立ちやすい。距離考慮のコストは3人規模なら無視できる。
### 型定義
```typescript
interface Reservation {
  entityId: string;      // TileId | ItemId | BuildingId（予約対象）
  jobId: JobId;
  colonistId: ColonistId;
}
// GameStateに追加するフィールド
interface GameState {
  // ...4章の定義に加えて
  reservations: Record<string /* entityId */, Reservation>;
}
```
`Job`型は4章で定義済み（`state: JobState`、`reservedBy`、`retryCount`、`cooldownUntilTick`を含む）。
### ライフサイクル
1. **ジョブキュー生成（JobGenerator）**: 毎tick、「伐採指定された木」「採掘指定タイル（岩・マナ結晶）」「未処理の運搬対象アイテム」「未完成のブループリント」「傷んだ建物」「狩猟／世話を指定された動物」「燃料を欲しがっている魔導炉」を走査し、対応する `pending` Job を生成する。重複防止は仕事の同一性キー（`jobKey`）で行い、同じ対象に二重の Job は作らない。
2. **候補フィルタ（CandidateFilter）**: 暇な（`currentJobId === null`）Colonistごとに、`pending`なJobの中から (a) 到達可能、(b) `reservations`に対象エンティティが存在しない、(c) 優先度が有効、(d) `cooldownUntilTick`を過ぎている、の4条件を満たすものを抽出し、優先度→距離の順でソートして先頭を選ぶ。
3. **予約（Reserve）**: 選ばれたJobについて、対象エンティティ（木・鉱石タイル・アイテム・建築フレーム）を`reservations`に登録し、Jobの`state`を`reserved`に、`reservedBy`をColonistIdに設定する。**運搬ジョブは搬出元アイテムと搬入先ゾーンの両方を予約する**（ゾーン側は「残り受け入れ可能容量」に対する予約）。理由: 搬入先を予約しないと、満杯間近のゾーンに複数のColonistが同時に運び込もうとして溢れる、あるいは置いた直後に別のColonistがまた運び出す無限ループが起きる。
4. **実行（Execute）**: `state`を`active`にし、Colonistの`currentJobId`にセットする。Job種別ごとの純粋関数（`ColonistとGameStateを受け取り、次のGameStateを返す`形）が、移動（7章の経路キャッシュを使用）→作業アニメーション→資源の増減、を1tickずつ進める。
5. **解放（Release）**: 完了時は対象エンティティを`reservations`から削除し、`state`を`completed`にしてJobキューから除去、Colonistの`currentJobId`を`null`に戻す。**失敗時**（経路が地形変更で完全に塞がれた等）は`retryCount`をインクリメントし、`cooldownUntilTick = tick + COOLDOWN_TICKS`（例: 50 tick）を設定して`pending`に戻す。`retryCount`が閾値（例: 3回）を超えたJobは`failed`として破棄し、ログに残す。理由: 無限リトライを防がないと、到達不能なジョブに毎tick候補フィルタの計算コストを吸われ続ける。
この5段階（生成→候補フィルタ→予約→実行→解放）が唯一のJob状態遷移経路であり、これ以外の経路でColonistがJobに触れることはない。
## 7. 経路探索
全Colonistが毎フレームA*を走らせると、60×60マスのグリッドでは即座にフレーム落ちする。**再計算のトリガーを「目的地変更時」と「地形変更時」の2つだけに限定する**ことで破綻を防ぐ。
### グリッドA*
- 60×60タイルのグリッド上で4方向移動（斜め移動はMVP範囲外、地形3種の見た目とコスト計算をシンプルに保つため）。
- ヒューリスティックはマンハッタン距離。
- コストは地形により変化させない（MVPでは草地・森・岩の移動コストは一律。森の減速等はスコープ外）。
### 経路キャッシュ
- Colonistは`path: Vector2[] | null`と`pathTargetTileId: TileId | null`を保持する（4章）。
- 新しい移動先が指定されたとき（＝Jobの予約が成立した瞬間、6章）だけA*を1回実行し、結果を`path`にキャッシュする。
- 以降のtickでは、Colonistは`path`の先頭から順に1マスずつ進むだけで、目的地が変わらない限り再計算しない。
- 複数Colonist間で経路そのものを共有するキャッシュ（同じ始点終点の経路を使い回す）は実装しない。理由: 3人規模ではA*1回のコストがそもそも小さく、共有キャッシュの整合性管理コストが見合わない。
### 地形変更時の無効化
- 建築（壁・扉の設置/破壊）や伐採・採掘で`Tile.walkable`が変化した場合、そのタイルを経路の一部として使っている全Colonistの`path`を無効化し、次tickで再計算させる必要がある。
- 全Colonistの`path`を毎回スキャンするのは非効率なので、**PathIndex**（`Record<TileId, ColonistId[]>`、「このタイルを現在の経路に含んでいるColonistの一覧」への逆引きインデックス）をGameStateとは別に（非セーブ対象の派生キャッシュとして）保持する。
- 地形変更時は、変更されたタイル自身についてPathIndexを引き、該当するColonistのみ`path = null`にして次tickの候補フィルタ／実行フェーズで再計算をトリガーする。理由: O(全Colonist)ではなくO(該当Colonistのみ)に抑えるため。
- 無効化範囲は「変更されたタイルそのもの」で十分とする。**隣接タイルまで広げない**。理由: 経路は明示的にそのタイルを通過する場合のみPathIndexに登録されており、隣接タイルの変化がその経路自体を壊すことはない（壁の設置は設置先タイルのwalkableのみを変える）。

### 到達可能性は A* で判定しない

候補フィルタ（6章）は毎tick「その仕事に行けるか」を問うが、そこで A* を走らせると本末転倒になる。歩行可能タイルの**連結成分ラベル**（`Int32Array`、地形変更時のみ再構築）を持ち、判定を O(1) にする。実際の A* は「実際に割り当てる候補」に対してのみ、1 tick あたり最大 `CANDIDATE_PATH_ATTEMPTS` 回だけ走る。

この設計には**鋭い縁**がある: 通行不可タイルの上に立ったエンティティは領域 −1 になり、地図上の全ての仕事が到達不能に見える。壁を建てて自分を閉じ込めた建築士がこれで生まれた（対策は notes 側）。

### 動物の経路

動物20〜40体が毎tick A* を走らせると3人規模で設計した予算が破綻する。

- 徘徊・逃走・採食は A* を使わない（隣接1マスの判断のみ）。
- A* を使うのは追跡と帰巣だけで、1 tick あたりの本数に上限を設ける（`animalPathBudget`）。群れが入植者の経路探索を締め出せない。
- 動物の経路は **PathIndex に登録しない**。代わりに最大 N tick で失効させる。厳密な無効化より、失効の方が動物には十分かつ安価。
## 8. セーブ／ロード
4章の原則（エンティティは純粋データ、ロジックは外部関数）を守っている限り、`GameState`はそのまま`JSON.stringify`できる。クラスのインスタンスやDate、Map/Setのような非JSON型は使わず、`Record<Id, T>`と配列とプリミティブのみで構成する。
### セーブファイル構造
```typescript
interface SaveFile {
  schemaVersion: number;   // マイグレーション判定用
  savedAtTick: number;
  savedAtRealTime: string; // ISO8601文字列（Date型は保持しない）
  state: GameState;
}
```
- `reservations`や`PathIndex`のうち、`PathIndex`は派生データ（7章）なのでセーブに含めない。ロード後、最初のtickで各Colonistの`path`から再構築する。
- `reservations`はセーブに含める（6章の補足の通り、ロード直後に予約が消えると事故が再発しうるため）。
### バージョニング方針
- `schemaVersion`を1から始め、`GameState`の型を破壊的に変更するたびにインクリメントする。
- ロード時は`schemaVersion`を見て、`migrations: Record<number, (old: unknown) => unknown>`のようなマイグレーション関数チェーンを、保存時のバージョンから最新バージョンまで順に適用する。
- 適用できないほど古い（マイグレーション関数が存在しない）セーブは、ロードを拒否してユーザーに警告を表示する。理由: 中途半端に読み込んで壊れた状態で進行させるより、拒否した方が安全。
### 保存先・タイミング
- 手動セーブ／ロードに加え、**1ゲーム内日ごとのオートセーブ**を別枠で持つ。
- 保存先はIndexedDB。理由: `items`や`jobs`が増えるとlocalStorageの5MB上限に近づきうるため、余裕のあるIndexedDBを既定にする。
- 現行 `schemaVersion` は **16**。v1〜v15 のセーブは読み込み時に順に移行される。移行チェーンは1段ずつテストしてある。
## 9. 機能リスト

MVP として定めた範囲と、そのあとに足したもの。**MVP の項目はすべて実装済み**で、いくつかは超過している。

### MVP（当初の範囲）

| 項目 | 当初 | 現在 |
| --- | --- | --- |
| マップ | 60×60・1枚 | **120×120・1枚**（寸法は `GameState` が持つので、旧セーブは 60×60 のまま） |
| 地形 | 草地／森／岩 の3種 | **4種**（マナ結晶を追加） |
| 入植者 | 3人 | 3人から開始（移住で最大8人） |
| ニーズ | 空腹・睡眠のみ | **3種**（娯楽を追加） |
| 仕事 | 5種 | **9種**（狩猟・世話・解体・修理を追加、優先度の列は7つ） |
| 建築 | 6種 | **17種**（石壁・石床・ベリー・霜花・魔導炉・導管・魔力灯・自動採掘機・炉端・タレット・交易柱を追加） |
| 資源 | 木材・石材・食料 | **4種**（マナ結晶を追加） |
| 速度 | 停止／1倍／3倍 | **4段**（10倍を追加） |
| セーブ | 手動・IndexedDB | 手動＋日次オートセーブ |
| UI | 入植者タブ・仕事優先度表・資源一覧 | 上記＋アラート・目標・ミニマップ・選択詳細・ログ・動物・貯蔵フィルタ |
| 勝敗 | なし。生存が続くことがゴール | 同じ |

### MVP のあとに足したもの

| 追加 | 概要 |
| --- | --- |
| 生物レイヤー | 動物**8種**の生態・狩猟・手懐け・牧場・繁殖・飼い葉（[design-phase2.5-animals.md](design-phase2.5-animals.md)、うち2種は11章フェーズ5） |
| 四季 | 1年20日。冬は作物が止まり草も戻らない |
| 災厄（インシデント） | 7種（襲撃を含む）。世界シードごとに別の暦を持つ |
| 移住者 | 食料に余裕がある植民地に3日ごと（冬以外）1人 |
| 熟練度・特性 | 7スキル・14特性（11章フェーズ3の先取り） |
| 気分・思考 | 導出値。作業速度と「仕事の放棄」に効く（同上） |
| 魔力インフラ | マナ結晶・魔導炉・導管・魔力灯・自動採掘機（11章フェーズ2、段階A〜C） |
| 幻想の生態と植生 | 霜花・光苔・晶角鹿・岩喰い（11章フェーズ5）。いずれもマナか季節か岩という既存の仕組みに噛み合う |
| 森の再生 | 伐採跡が戻る。地図ごとの森林容量で頭打ち |
| シナリオ | 開始条件3種 |

### 実装していないもの

- 入植者間の関係性、複数種のメンタルブレイク（同フェーズ3）
- 空腹・睡眠以外のニーズ（娯楽・快適・美しさ・屋内外）
- 複数マップ・キャラバン（交易・研究ツリーは実装済み、キャラバンは非目標）
- 天候・気温管理（季節はあるが気温は無い）
- 健康画面・部位・病気（`health` は0..100の単一値のみ）

**当初「含まないもの」としながら実装した項目**は、気分・メンタルブレイク・熟練度・特性（いずれもフェーズ3の先取り）、最小構成の `health`、そして季節。理由は [design-notes.md](design-notes.md) に記録してある。

## 10. 実装順序

土台（状態設計・描画・経路探索）を先に固め、ジョブシステムをその上に積み、UI は最後に被せる。理由: 予約機構（6章）は見た目より先に正しく動く必要があり、後から差し込むのが最も難しい部分だから。

この順序は実際に守られ、MVP は Week 1〜7 相当の区切りどおりに積み上がった。各段階の「動いたと言える条件」は以下で、すべて対応するテストになっている。

| 段階 | 内容 | 動いたと言える条件 | 対応するテスト |
| --- | --- | --- | --- |
| 1 | 基盤とデータ設計 | 空の `GameState` が JSON 往復で一致する | `roundtrip.test.ts` |
| 2 | マップと描画 | 60×60が表示され、パン・ズームできる | 実ビルドでの確認 |
| 3 | 経路探索と手動移動 | クリックで壁や森を迂回して到達する | `pathfinding.test.ts` |
| 4 | ジョブ中核 | 大量の伐採指定でも2人が同じ木に向かわない | `jobs.test.ts` |
| 5 | ニーズと生存ループ | 無操作で食事・睡眠を取り、餓死しない | `survival.test.ts` |
| 6 | 建築と React UI | 壁の設置指示で資材運搬から完成まで通る | `actions.test.ts` |
| 7 | 時間モデルと通しプレイ | 無操作・3倍速で複数日、行き詰まらない | `longrun.test.ts`（20日） |

以降の追加も同じ形を踏襲する——段階ごとに「動いたと言える条件」を先に決め、それをテストにしてから実装する。

## 11. フェーズ2以降のロードマップ

コアの魅力の優先順（1. 最適化パズル、2. 物語生成、3. 防衛と危機管理）に対応させ、魔力インフラ→物語生成→防衛の順で積む。依存関係として、フェーズ4の魔力式防衛設備はフェーズ2の魔力インフラを前提にする。**この依存順を崩さない**。

フェーズ3のうち特性・気分・思考だけは、フェーズ2より先に実装した（入植者を「二本のゲージを持つ機械」から個人にするのに、関係性やメンタルブレイクの一式は要らなかったため）。依存順を崩したわけではなく、依存の無い部分を先に取っただけ。

### フェーズ2: 魔力インフラ — 段階A〜C 実装済み

既存の生産チェーンに「電力」に相当する魔力の生産・分配という制約を追加する。**制約の質が違う**のが要点で、材料が足りるかではなく供給し続けられるか、を問う。

| 項目 | 状態 | 実装 |
| --- | --- | --- |
| `manaCrystal`（`ResourceType` に追加） | 済 | 地形 `crystal` を岩盤の奥に配置。1本6産出 |
| マナ結晶の採掘 | 済 | **新規ジョブを作らず既存 `mine` の対象拡張で対応した**。当初の「対応可能か検討」への答え |
| `manaFurnace`（魔導炉） | 済 | 石25＋木10。結晶1個で2/3日燃え、10供給 |
| `manaConduit`（導管） | 済 | 石2。歩ける——送電線が回り道を強いる壁になってはいけない |
| `ManaNetwork` / `ManaGrid` | 済 | **導出**。連結成分ラベリングで `SimContext` に置き、セーブしない |
| `manaConsumption` / `manaOutput` | 済（形は変更） | `Building` のフィールドではなく**種類ごとのテーブル**。建築コストと同じ理由——種類の性質であって個体の性質ではない |
| 魔力灯（`ManaConsumer` の1つ目） | 済 | 3消費。半径6タイルの入植者の気分 +5 |
| 自動採掘機（同2つ目） | 済 | 8消費。半径3の岩を500 tickに1タイル、入植者なしで掘る |
| 温度管理・防衛タレット | 未 | 前者は気温が無いので保留、後者はフェーズ4 |

**段階A〜C 完了。** フェーズ2の残りは温度管理（気温の概念が無いため保留）とタレット（フェーズ4）だけ。

**供給不足はグリッド全体を落とす**（部分的なブラウンアウトにしない）。理由は [design-notes.md](design-notes.md)。

### フェーズ3: 物語生成 — 実装済み

| 項目 | 状態 |
| --- | --- |
| `traits`（性格特性） | 済。14種・7排他グループ。すべて既存の数値への乗数 |
| `mood` | 済。ただし**保存せず導出**する |
| `thoughts` | 済。同上。パネルは計算に使った思考をそのまま並べる |
| `MentalState` | 済。3種（brooding / wandering / binge）。**引き金になった思考で決まる**ので、見た目から理由を逆算できる |
| `Relationship`（入植者間の affinity） | 済。近くで過ごすと育ち、離れると緩やかに薄れる。**仲間の存在**と**喪**の2つとして気分に効く |
| 娯楽（recreation） | 済。2日で満杯、炉端で解消。快適・美しさは思考として気分に載っており、ニーズにはしていない |

### フェーズ4: 防衛と危機管理 — 実装済み

| 項目 | 状態 |
| --- | --- |
| `RaidEvent`（襲撃） | 済。8日目以降、災厄の1種として発生。地図の端から入り、壁を壊し、半日で引き上げる |
| `CombatState` | 済。`activity: fighting` として実装。**狩猟列が有効な入植者**が民兵になり、それ以外は逃げる |
| 防衛タレット | 済。魔力6を消費し、射程7で自動射撃。**グリッドが点いている間だけ**動く |

戦闘状態を `Colonist` の新フィールドではなく `activity` の一種にしたのは、仕事・気分・ニーズの各系統が
すでに `activity` を見て動いているから ── 新しいフィールドを足すと、それを見落とす場所が生まれる。

民兵に専用のUIを作らなかったのも同じ理由。**危険な動物を任せている人**がそのまま襲撃にも出る。

### フェーズ5: 交易とファンタジー層 — 実装済み

当初のロードマップ（2→3→4）に無い追加。設計案は [design-phase5-trade.md](design-phase5-trade.md)。

この世界は 1 章で**ファンタジー世界観**と定めているが、実装されている幻想要素は魔力インフラだけで、地形・植生・動物はいずれも現実のものである。フェーズ5は、既にある唯一の幻想要素（マナ）を生態と植生に接続し、そこへ**交易**という2つ目の資源の出入り口を足す。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 行商人・交易柱・物々交換 | 済 | 商人が交易柱の隣に現れ、1日立って去る。取引は既存の `haul` で成立し、`JobType` は増えない |
| 晶商（マナ結晶の売買） | 済 | 供給の通った魔力灯が来訪を格上げする。フェーズ2 段階B が前提 |
| 霜花（冬にだけ育つ植物） | 済 | `FROSTBLOOM_GROWTH_BY_SEASON`（冬 1・他 0）を引く。岩の際の草地に14株、収穫は `food` 7（ベリーは9） |
| 光苔（灯りの下で `forage` が戻る） | 済 | 供給の通った魔力灯の半径6内は、季節に関係なく `forage` が夏草の0.6倍で回復する |
| 晶角鹿（`manaCrystal` を産む家畜） | 済 | `SPECIES` に1行と `produceType` の1フィールド。2400 tick に1個＝1日1.25個、炉の消費（1.52個）を**下回る** |
| 岩喰い（岩を食う動物） | 済 | 3つ目の食性 `lithovore`。900 tick で岩1タイルを開ける。鉱脈は食わず、**その手前の岩を開けて露出させる** |

**ファンタジー層の4種はどれも新機構ではない。** 霜花は既存の建築型＋季節表1枚、光苔は既存の
`Tile.forage` が回復する条件の追加、晶角鹿は `SPECIES` の1行、岩喰いは食性の3つ目である。
`ResourceType` も `JobType` も `GameState` のフィールドも増えていないので、**セーブの移行は不要**
（`schemaVersion` は交易の 16 のまま）。

**依存順**: フェーズ2 段階B（実装済み）が前提。フェーズ3・4 には依存せず、**フェーズ4 より先に着手してよい** —— 番号は「元のロードマップの外にある追加」を示すものであって、着手順ではない。キャラバン（入植者が地図の外へ出る形）は引き続き**非目標**で、土台の「1枚の地図」を変えるため。

**ドキュメントの命名**: フェーズ単位で足す層の設計案は `design-phase<番号>-<名前>.md` とする（生物レイヤー＝`design-phase2.5-animals.md`、交易＝`design-phase5-trade.md`、空間＝`design-phase6-space.md`、時間＝`design-phase7-time.md`、装備＝`design-phase8-equipment.md`）。`design.md` / `design-notes.md` / `design-next.md` の3つは特定のフェーズに属さないので、この命名は適用しない。

### フェーズ6: 空間（マップ拡張と画面の再配置） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase6-space.md](design-phase6-space.md)。**ゲーム内容を1つも増やさず**、盤面と見え方だけを変える。他のどのフェーズにも依存しない。

MVP 当時の 60×60 の地図と、幅 300px に11個のパネルを縦積みした右サイドバーは、入植者8人・仕事9種・建築17種・動物8種・牧場・魔力網が載った現在の植民地に対して狭い。両者は独立ではなく、地図が広がればミニマップとアラートからの移動の重みが増して画面の奪い合いが厳しくなる。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| マップ寸法を `GameState` へ | 済 | `state.width` / `state.height`。`MAP_WIDTH` / `MAP_HEIGHT` は `DEFAULT_MAP_*` に改名し、**新しい世界を作るときの既定値**だけを意味するようにした |
| 60×60 → 120×120（面積4倍） | 済 | 既定が 120×120。実測 1 tick 6.4 ms（予算 200 ms の 3.2%）、セーブ 1.95 MB |
| 絶対数を密度へ | 済 | `perArea` / `perSpan`。ベリー・霜花・野生動物・湧き出し距離が面積（短辺）から決まる。**狼の上限だけは難易度側に残す** |
| マップサイズの選択 | 済（A-4） | 狭い谷60×60・辺境120×120（既定）の2種。TopBar で次の地図の広さを選ぶ。設計案の大荒野180×180は**実測27.9ms/tick で不採用**（design-notes.md「マップサイズ」） |
| 範囲選択（矩形ドラッグ） | 未 | 機構は既にあり、`renderer.ts` の Select 除外を外す作業 |
| 選択の中身を地図の近くへ | 未 | いまは目が地図を見ているのに答えは画面右端に出る |
| 選択詳細の3パネルを1枠へ | 済 | `SelectionFrame`。3つは store 上で排他なので、枠は1つでよい |
| サイドバーの折り畳み | 済 | `Fold` と `localStorage`。**`GameState` には入らない**（テストで機械的に固定） |
| 資源・アラートを左上、ミニマップを右上へ | 済 | オーバーレイ。アラートは0件のとき**何も描かない** |

地形の構成比は 60×60 と 120×120 で1ポイント以内で一致するため（20 シード実測）、**地形生成には手を入れていない**。実測値は [design-phase6-space.md](design-phase6-space.md) の 2 章と、実装後の値は [design-notes.md](design-notes.md)。

**セーブは `schemaVersion` 17。** 移行は `width: 60, height: 60` を補うだけで、
**既存のセーブは 60×60 の世界としてそのまま遊べる**。新しい世界だけが 120×120 で生成される。

**テストは 60×60 のまま回す。** 4倍の地図は4倍の実行時間で、20分かかるテストは走らなくなる。
それ以上に、[design-notes.md](design-notes.md) の実測値はすべて 60×60 で取ったもので、
ハーネスが黙って広がると**それらが一斉に無効になる**。出荷される寸法は
`longrun`（120×120 で1年）・`chaos`（120×120 で不変条件）・`roundtrip`（2つの寸法を同一プロセスで往復）が受け持つ。

選択もこのフェーズで扱う。現在の Select は1マス単位で、結果は画面右端の `SelectionPanel` に出る（**目は地図を見ているのに答えは右端に出る**）。矩形ドラッグの機構は既にあり `renderer.ts` が `tool.kind !== 'select'` で Select だけを除外しているので、範囲選択は**除外を外す作業**になる。中身は選択の近くにポップアップで出し、重い情報（入植者シート）はサイドバーに残す。

### フェーズ7: 時間と動き（昼夜の表現・なめらかな移動） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase7-time.md](design-phase7-time.md)。前提となるフェーズは無い。

2つとも**必要なものは既にシミュレーションの側にあり、足りないのは画面の側だけ**だった。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 補間の速さを歩調から導く | 済（M-1） | `interpolationSpeed`（`src/render/pace.ts`）が `ticksPerStep × TICK_MS ÷ 速度` から毎フレーム導出。1倍速は400msかけて等速で渡り、停止中は滑らない。3マス超の跳び（退避・ロード）はスナップ ―― ただしこの時点では入植者（`updateColonistView`）にしかガードが無く、動物・襲撃者への拡張は本章末尾のプレイテスト由来の修正（#8）まで持ち越された |
| 動物の補間を種ごとに | 済（M-2） | 同じ式に種ごとの `ticksPerStep` を入れるだけ。ニワトリ（4）はシカ（3）より遅く見える |
| 状況で歩速を変える | 済（M-3） | 荷1.15倍・森1.25倍・負傷最大1.4倍（すべて遅く）・逃走1.2倍速い。乗算合成で遅さ上限2.0。`Colonist.stepProgress`（オプショナル、無ければ0、移行不要）が歩の端数を持つ |
| 昼夜のオーバーレイ | 済（N-1） | `shadeAt`（`src/render/daylight.ts`）: キーフレーム線形補間で夜0.45の濃紺〜昼0、境界で跳ばない。全面1枚でタイル数に依らず一定コスト |
| 夜に魔力灯が明るい | 済（N-2） | 給電中の灯だけ `litLamps` が返し、放射グラデーション（コード生成）を加算合成。**燃料が切れるとグリッドごと暗くなる**。フェーズ2の見返りが初めて目に見える |
| 夜に寝やすくする | 済（N-3） | `SLEEP_THRESHOLD` に時刻乗数（夜0.7・昼1.3、傾斜は20〜22時と05〜07時）。**乗数だけでは寄らないことが実測で出たため、「夜の眠りは夜明けまで続く」（空腹85超で覚醒）を追加**。夜間（19〜05時）の睡眠比率 0.64〜0.67（3シード、一様なら0.42）。実装メモと design-notes 参照 |

### フェーズ8: 装備（服と武器） — E-1〜E-4・E-6 実装済み（E-5 のみ保留）

当初のロードマップに無い追加。設計案は [design-phase8-equipment.md](design-phase8-equipment.md)（末尾に実装メモ）。

| 部分 | 状態 | 内容 |
| --- | --- | --- |
| 装備の土台（E-1） | 済 | `GameState.equipment` の1レコード（`schemaVersion` 23→24）。参照は `wornBy` の一方向のみで「誰が何を持つか」は導出。作業台の注文列（`Building.craftOrders`、選択パネルのボタン）→既存 haul で材料搬入→`craft` ジョブで作成→**手の空いた入植者が直接拾って装備**（装備専用の運搬チェーンは作らない。予約なし・先着） |
| 作業具（E-2） | 済 | 斧=伐採0.8倍・鶴嘴=採掘0.8倍。`putInWork` でスキル・気分と掛け算。装備ゼロなら導入前と一致（テストで固定） |
| 狩猟具（E-3） | 済 | 弓=射程5→8（猪の突進6の外から狩れる）・槍=狩猟の作業0.75倍 |
| 耐久（E-4) | 済 | 使った回数だけで減る（斧120回・鶴嘴100・弓40・槍60・剣60・鎧80）。0で壊れて**ログ＋作業台へ自動再注文**（黙って消えない） |
| 防寒着（E-5） | **保留** | 材料が未決のまま（設計案8章の判断を尊重）。冬の負乗数もセットなので入れていない |
| 戦闘装備（E-6） | 済 | 剣（木5+鉄8）=民兵の一撃1.5倍・鉄の鎧（鉄15）=被弾0.6倍。既存の襲撃の数値に乗るだけで、装備ゼロなら現行の襲撃と同じ |

### フェーズ9: 言語（日本語表示と言語切り替え） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase9-language.md](design-phase9-language.md)。
**ゲーム内容を1つも増やさない**（フェーズ6と同じ質の、見え方だけのフェーズ）。前提となるフェーズは無い。

| 項目 | 状態 | 実装 |
| --- | --- | --- |
| 辞書機構と言語切り替え | 済 | `src/ui/strings.ts`（`Strings` インターフェースが全キーを型で強制、`Record<Language, Strings>`）と `src/ui/language.ts`（zustand の小ストア）。ライブラリなし。言語設定は `localStorage`（`GameState` に入れない）。切替は TopBar のシナリオ選択の隣 |
| 静的UI文言の辞書化 | 済 | JSX 直書きと散在していたラベル表を辞書に統合。建物名の二重定義（Toolbar / SelectionPanel）は消えた |
| 導出文言の構造化 | 済 | アラート・目標・状態メッセージは `{key, params}` を返し、文章は表示の瞬間に辞書から導出。既に出ている文言も切り替えで即座に翻訳される |
| ログの構造化 | 済 | `LogEntry` は `{tick, kind?, key, params}`。移行（v15→v16）は旧エントリを `legacy` に包んで原文のまま表示する |

CLAUDE.md の言語方針は「UI 文言は辞書キー経由（辞書に en / ja）」に改定済み。
コード・コメント・識別子・キー名は英語のまま。

### フェーズ10: 鉱石と家具 — 実装済み

当初のロードマップに無い追加。設計案は [design-phase10-ores.md](design-phase10-ores.md)。前提となるフェーズは無い。
鉱石を足す → 使い道として家具を足す → 建築メニューが溢れる → 階層化する、という1本の因果で3つの変更が繋がる。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 鉱脈の一元表（`VEIN_YIELD`） | 済 | 採掘産出の分岐2箇所（`execute.ts` / `mana.ts`）が1枚の表を引く。素の岩は行ではなくフォールバック（stone 20）。以降の鉱石追加は「表に1行」 |
| 鉄（`iron` / `ironVein`） | 済 | 結晶より浅く多い2つ目の鉱石。生成は `s > 0.78` かつ鉄ノイズ `> 0.57`（結晶判定が先に走る。200シード実測は [design-notes.md](design-notes.md)）。移行は v9→v10 と同型（v18→v19） |
| 家具5種 | 済 | 食卓（木10+鉄2、半径2の食事に思考+3）・腰掛け（木6、食卓に隣接で+3→+4）・戸棚（木15+鉄4、半径2のベッドの睡眠回復×1.15、重複せず特性と乗算）・安楽椅子（木12、`relaxing` の対象拡張で回復×1.3）・石像（石15、半径4に思考+3）。半径はすべてチェビシェフ距離。効果値は種類ごとの定数表（`constants.ts`）にあり `Building` には載せない。新しいニーズ・ジョブ・`GameState` フィールドは無し。マイナスの思考は入れない。設計図は無効果、解体は既存の半額返却 |
| 建築メニューの階層化 | 済 | カテゴリ切替式（構造6/家具7/魔力5/区域3、`src/ui/buildMenu.ts`）。表示ボタン数がカテゴリ内最大数（7 = 家具）で頭打ちになる。表示名は辞書（フェーズ9）と共有 |

### フェーズ11: 世界（ワールドマップ・バイオーム・部族） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase11-worldmap.md](design-phase11-worldmap.md)。
**「1枚の地図」の土台は守る** —— ワールドマップは生成時の選択画面であって第二の盤面ではなく、
プレイ中の複数マップ・キャラバンは引き続き非目標。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| バイオーム4種 | 済（段階A） | 草原（現行の生成そのもの）・深森・岩尾根・晶土。シナリオと同じ「レバーの束」で、地形生成の閾値と密度だけを曲げる。design-next 提案1(a)（鉱脈の下限保証）を全バイオームの下限として実装した。`GameState.biome` は直接保存されたまま（段階Bでも導出キャッシュへの移行はせず、選んだセルから生成時に一度だけ書く形を維持。実装メモに詳細）。実測値は design-notes.md「バイオーム（フェーズ11 段階A）」 |
| ワールドマップ | 済（段階B） | 16×16セル、世界レベルの値ノイズ2枚（湿り気・険しさ）の閾値からバイオームを導出。**`worldSeed` から導出し、保存しない**。`GameState` に入るのは選んだセル座標 `worldCell` だけ（`null` = 導入前のセーブ）。ローカルマップのシードは `cellSeed(worldSeed, x, y)`。TopBar の「New map」がワールドマップのオーバーレイを開き、セルをクリックして「ここで始める」、または「おまかせで始める」で従来の1クリック開始も残る。プレイ中は同じオーバーレイを閲覧専用（選び直し不可）で開ける |
| 部族3つ | 済（段階C） | 灯持ち（晶土に集まる。交易の出どころ）・森歩き（深森に広がる。移住の出どころ）・渇き衆（岩尾根を彷徨う。襲撃の出どころ）。実体を持たず、勢力圏はその部族が好む世界セルの集合そのもの（別ノイズは持たない）。選んだセルからの距離が既存の頻度・規模への乗数になる（渇き衆＝襲撃規模×1.5・重み×1.5、灯持ち＝行商間隔5日→3日、森歩き＝移住間隔3日→2日）。実測値は design-notes.md「世界地図と部族（フェーズ11 段階B・C）」 |

世界観の規律はフェーズ5と同じ ——「マナとどう付き合うか」の違いだけで部族を分け、
エルフ・ドワーフのカタログは持ち込まない。渇き衆は「マナは唯一再生しない資源」という機構の
帰結を物語の側から言い直したもの。

### フェーズ12: 研究と職業 — 実装済み

当初のロードマップに無い追加。設計案は [design-phase12-research.md](design-phase12-research.md)。
**フェーズ10（家具）の段階Bが前提**（研究が解禁するものが先に要る。実装済みなので前提は満たされている）。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 研究台と `research` ジョブ | 済 | 仕事の列は 7 → 8（`JOB_TYPES`）。スキルも8種目に `research` が増えた。既定優先度は3（最低）。資源要求テック（晶学）は魔導炉の燃料補給と同型 —— 研究台の `requiredResources` をブループリントと同じ場に載せ、既存の `haul` ジョブがそこへ運ぶ。研究者の徴用は民兵と同型（`research` 列が有効な入植者が研究者になるだけで、専用の徴兵UIは無い） |
| 研究ツリー | 済 | 4テック・2段（木工の心得・石彫の心得・鉄工の基礎・晶学の初歩）。**既存の建築22種（食卓・腰掛け含む）はロックしない**（解禁するのは安楽椅子・戸棚・石像の3種のみ）。未解禁は建築メニューに灰色＋必要テック名で表示し、`placeBuildingBlueprint` も `isUnlocked` で同じ規則を強制する（UI迂回を防止） |
| 職業 | 済 | 保存フィールドにしない。肩書き（`titleSkillOf`）は最高スキルからの**導出**（同率は `SKILL_NAMES` の先頭勝ち）、職業プリセット（6種）は優先度表への**UI の一括設定**。保存されるのは適用結果の `workPriorities` のみ |

研究ツリーは design-next.md 6 章で「提案しない」側に置かれていたが、複数マップと違い
**変えるのは時間の形だけで、盤面は1枚のまま**であること、建築が20種に増えて
「最初から全部建てられる」ことが提示の失敗になりつつあることから、判断を改めた（経緯は同6章）。
実測したテックのコストは design-notes.md「研究と職業（フェーズ12）」を参照。

### フェーズ13: 提示（画面の整えと音） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase13-presentation.md](design-phase13-presentation.md)。
実プレイの指摘5件から出たフェーズで、前半はフェーズ6（画面の再配置）の続き、後半は初の音の層。
**`GameState` に増えたフィールドはゼロ**（すべて表示の性質。開閉・音量ミュートは `localStorage`、
`schemaVersion` は動いていない）。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| ステータスの定位置化 | 済（段階A） | TopBar 1行目の常設スロット（`flex: 1` + `min-width: 0` + 省略記号、全文は `title`）。メッセージの有無で `app__body` は 1px も動かない（実ビルドで測定。design-notes.md「提示（フェーズ13）」） |
| オーバーレイの折り畳み | 済（段階B） | 資源・ミニマップ・選択枠に既存 `Fold` を適用（`PanelId` に `selection` を追加しただけ）。資源・ミニマップは自前の `section`/`h2` をやめ、Fold が見出しを持つ他パネルと同形に。**アラートは畳みの対象外**（`AlertPanel` は Fold の外） |
| 選択枠のビューポート内表示 | 済（段階B） | `SelectionFrame` は盤面左下 `.overlay--bl`（折り畳みあり）。何も選んでいないときは枠ごと描画しない。サイドバーは植民地全体を見る場所に純化。タイル詳細の見出しは「タイル x, y」（Fold の「選択中」と重ねない） |
| 効果音（実行時合成） | 済（段階C） | Web Audio で6種（配置・完成・警告・襲撃の角笛・研究完了・取引成立）。パラメータ表 `SFX`（`src/ui/sfx.ts`）がアセットそのもので音声ファイルは0バイト。発火は状態の**差分**からのみ（`SfxDirector`）：ログの新規行・critical アラート集合の増分・ブループリント/建物数の増分。同種の最短間隔 500ms、tick が50超跳んだら（＝ロード）無音で基準を取り直す。既定ミュート、トグルは TopBar の言語切替の隣。BGM と Diffusion Audio（生成モデル）はこの時点では保留（同設計案 5.2 の再検討条件つき）——**その後フェーズ15で保留は解けた**。5.2 が挙げた再検討の2案のうち **(a) ローカル生成モデルで作ったループをサブセット同梱する** を採り、BGM も入った。ただし**モデルを実行するのはビルド前の開発者の手元**であって、ゲームは生成モデルを積んでいない（本章「フェーズ15」参照） |

「夜も明るい」問題は本フェーズではなく**フェーズ7の昼夜オーバーレイの領分**
（design-phase13-presentation.md 6 章に切り分けを記載。新たに設計するものは無い）。

### design-next 提案2・提案3 — 実装済み

フェーズ番号を持たない [design-next.md](design-next.md) 由来の2件。決め手と実測は design-notes.md。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| マナの目標とアラート（旧提案2） | 済 | 目標2件: 「マナ結晶を掘る」（在庫か燃焼中の炉から導出）・「灯りを点す」（給電中の灯から導出。燃料が切れると未達に戻る）。アラート2件: 結晶切れの炉（原因側）・過負荷グリッド。1つの問題に1つのアラート、クリックで現場へ飛ぶ。遊休グリッド（需要0）は鳴らさない |
| 作業台と調理（旧提案3） | 済 | `workbench` 1種・`craft` 列（既定優先度3）・レシピ1本: 生の食料10→調理済み10。`Item.variant: 'meal'`（`ResourceType` は増やさない）。スタック統合・貯蔵容量・運搬は variant 別で、生と調理済みは混ざらない。調理済みは満腹回復70→95＋「まともな食事」+6（半日）。供給は研究台と同型（`requiredResources`＋既存 haul）。生の食料が入力10+予備10を超えるときだけバッチを開く。`schemaVersion` 22→23 |

なお design-next 提案1のうち (a) 下限保証はフェーズ11段階Aで実装済み、**(b) 鉱脈の再生は未実装のまま**
（フェーズ5の再生経路2本により優先度は下げられている。経緯は design-next.md 5章）。

### プレイテスト由来の修正5件（#8・#9・#11・#13・#14）— 実装済み

フェーズ番号を持たない、実プレイテストで見つかった不具合5件。決め手と実測は design-notes.md。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| サイドバーのスクロールバーのちらつき（#11） | 済 | `.sidebar` と `.log--scroll` に `scrollbar-gutter: stable` を追加（`src/styles.css`）。コンテンツ高がビューポート高（`.log--scroll` は自身の `max-height`）を跨ぐたびにスクロールバーが出入りし、内容幅が横に揺れていたのを止めた。`.modal`（ワールドマップ）には**適用しない**――開いている間に高さが変わらず、固定幅も持たず `max-width` までコンテンツに合わせて縮む箱なので、gutter を常時確保すると逆に幅が変わる |
| ミニマップのドラッグ（#14） | 済 | `onClick` を `pointerdown`/`pointermove`/`pointerup`/`pointercancel` に置き換え（`src/ui/Minimap.tsx`）。座標変換を純粋関数 `tileAtMinimapPoint` として切り出し（範囲外はクランプ）、`setPointerCapture` でミニマップ矩形の外へポインタが出てもドラッグが続く。`focusOnTile` は毎 `pointermove`、`selectTile` は `pointerup` の1回だけ――ドラッグ中に毎回選択すると検査パネルが通過タイルを次々表示してちらつくため。ドラッグ状態は `useRef`（`GameState` には入らない） |
| 動物のクリック選択（#13） | 済 | 純粋関数 `pickAt(state, x, y)`（`src/render/pick.ts` 新規）を新設し `handleSelectClick`（`src/render/renderer.ts`）から使う。優先順は 入植者 → 動物 → 移動命令 → 選択解除。入植者と動物が同じタイルにいれば入植者が勝ち、動物が複数いても id 順で決定的に1体を返す。入植者を選択中に動物をクリックしたら、移動命令ではなく動物の選択にした――逆にすると、その場から動かない動物（膠着した狩猟対象・追い詰められた捕食者）が永久にクリックで選べなくなるため。代償は「動物のいるタイルへ直接クリックで歩かせられない」（1マス隣のクリックで済む） |
| 開始直後の高速移動（#8） | 済 | 報告された症状自体はフェーズ7で既に解消済みだった。残っていたのは2つの欠陥：(1)「3マス超離れたらスナップ」のガードが `updateColonistView` にしか無く `syncAnimals`/`syncRaiders` に無かった（退避やロードで歩いてもいない地面をスプライトが横断していた）。(2) catch-up の上限で打ち切ったぶんの時間を `accumulator` に残していたため、ストールが明けても毎フレーム上限に張り付き続けていた。両方を修正し、テレポート判定を `isTeleport`/`TELEPORT_DISTANCE_TILES`（`src/render/pace.ts`）に1箇所へ出して3種（入植者・動物・襲撃者）で共有、catch-up の計算を `advanceTicks`（`src/game/loop.ts`）として切り出してテスト可能にした。定数（`MAX_CATCHUP_TICKS`・`MAX_FRAME_MS`）は変えていない |
| 狩猟時に入植者がうろうろする（#9） | 済 | 原因は2つあり、当初の仮説より重いものが主因だった。**主因**: `chase` の貪欲移動は「大きいほうの軸→だめならもう一方の軸」を試し、両方が塞がったときだけ実経路（A*）に落ちていたため、1タイルより幅のある岩場で壁面を往復し続け狩猟が完了しないことがあった。貪欲移動は大きいほうの軸だけを試し、そこが塞がっていれば直ちに実経路へ落ちるよう変更（`src/core/movement.ts`）。求めた経路は既存の `path`/`pathTargetTileId` に載せて最後まで歩き切る。**副因**（当初の仮説どおり実在）: 射程ちょうどで `chase` が `'arrived'` を返して止まるため、獲物が1歩さまようたびに射程外へ出て詰め直す往復があった。`HUNT_APPROACH_MARGIN = 2` を追加し、最初の作業tickが入るまでは `huntRangeOf() - 2` まで深く詰める（`src/core/jobs/execute.ts`・`src/core/constants.ts`。弓・近接の射程性質は保たれるよう下限をクランプ）。新しいフィールドは増やしていない（既存の `path`/`pathTargetTileId` を使う）。**この変更は狩猟・手懐けにだけ効かせる**――`chase` は `raid.ts` の守備側戦闘（射程1）とも共有されており、毎tick再照準して動く襲撃者に対して古い経路を歩き切ると接敵が遅れて被害が増え、`longrun` の seed 8117 が全滅した（7シード実測、design-notes.md）。`ChaseOptions.persistDetour`（既定 `true`）を足し、`raid.ts` の呼び出しだけ `false` を渡して #9 以前の挙動へ戻してある |

`#8`・`#9` はどちらも「主張する前に測る」（CLAUDE.md）の実例で、報告された症状に対する当初の仮説が
部分的にしか当たっておらず、より重い原因が別に見つかった。測定条件と実測値は design-notes.md。

### フェーズ14: 水と薬草と医療 — 済

当初のロードマップに無い追加。設計案は [design-phase14-water-medicine.md](design-phase14-water-medicine.md)。
実プレイからの要望（#16）が発端で、**design-next.md 6章の「医療・部位・病気は非目標」を撤回した**
（撤回したのは「病気という層を持たない」ことだけで、損傷を `health` 1つに固定する規律は維持する）。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 水地形（浅瀬・深水） | 済（段階 W-1） | `TerrainType` に2種。**深さは `Tile` の新フィールドにしない**――通行可否は `makeTile` の既存経路（`walkable: !isRock(terrain) && terrain !== 'deepWater'`）にそのまま乗り、`walkable` を書き換える既存6箇所が「深さも見る」必要を持たない。浅瀬は歩けるが建てられない、深水はどちらも不可。**`isRock` には入れず `isWater` を別に立てた**――`isRock` は「通行不可」「採掘対象」「建築不可」を兼ねており、入れると `veinYieldOf` の既定で水を掘ると石が出る。建築のガードは `actions.ts` の1箇所だけなのでそこに足す。`pathfinding.ts` は1行も触っていない（`isWalkable` は `Tile.walkable` を読むだけで `terrain` を見ない）。生成は5本目のノイズ＋`BiomeProfile.waterThreshold`（4バイオームの行が埋まるまで型が止める）で、**水を岩・森より先に決める**（湖の中に岩壁が立たない）。深水はノイズの芯だけ、縁が浅瀬なので**湖の岸は必ず歩ける**。`schemaVersion` 24→25、移行は**何もしない**――水のレトロフィットは入植者が立っている・建物が建っているタイルを後から通行不可にする形で、移行は `createSimContext` の前に走るので region を張り直せない。実測値は design-notes.md「水（フェーズ14 段階 W-1）」 |
| 川と連結性の保証 | 済（段階 W-2） | 川の本体は浅瀬にして「渡れる」を既定にする。加えて生成の最後に拠点 region の下限（90%）を保証する（`enforceConnectivity`、`enforceCrystalFloor` と同じ「生成してから足す」形）。**測ったところ断片化は水由来ではなかった**――岩尾根が元から持っていた性質で、保証は水だけでなくこの既存の断片化にも効いた。`schemaVersion` 25→26は無し（W-2自体は地形生成のみでセーブ形式を増やさない）。実測値は design-notes.md「川と連結性（フェーズ14 段階 W-2）」 |
| 薬草 | 済（段階 H-1） | 水辺の草地に生える3つ目の野生植物。産出は新 `ResourceType 'herb'`（`food` の variant にすると腹を空かせた入植者が薬を食べる）。3種目を `WILD_PLANTS` 表に抽出（フェーズ10の `VEIN_YIELD` と同じ形）。`schemaVersion` 25→26、移行は既存の備蓄ゾーンの `accepts` に `herb` を足す1段。実測値は design-notes.md「薬草（フェーズ14 段階 H-1）」 |
| 病気・治療・`treat` スキル | 済（段階 M-1） | `Colonist.illnessTicks`（任意フィールド、`mealUntilTick` と同形。`worldgen.ts` を触らずに新しい入植者の既定値「無し＝健康」を成立させるための選択）。`JobType` に `treat` を足すと `SkillName = Exclude<JobType, ...>` によりスキルが自動的に生える。発症は既存のインシデント枠（`events.ts`）に1本、頻度は季節重みと `illnessWeightMultiplier`（気分・空腹・頭数）で調整。治療は既存のジョブ機構1本（`treat`）――患者へ移動して`herb`を1個消費し、1回の治療で全快する。体力は既存の負傷・餓死と同じ経路（`health` 減算 → 0で `killColonist`、死因は既存の汎用キー `colonistKilled`）に乗せ、新しい死に方は増やしていない。アラート1件（`colonistsIll`）と思考1件（`sick`）を追加。`schemaVersion` 26→27、移行は `workPriorities.treat`/`skills.treat` の既定値補完と `illnessTicks` の0埋め。実測値は design-notes.md「病気と治療（フェーズ14 段階 M-1）」 |

### プレイテスト由来の追加3件（#12・#15・#19）

フェーズ番号を持たない、実プレイテストで出た要望3件。どれも**表示だけ**を変え、
`GameState` にもセーブにも1フィールドも増えない（`schemaVersion` は動かない）。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 地形のバリエーションと農地の見た目（#19） | 済 | 草地3種・森3種・岩2種。**Codex CLI は使わず既存パイプライン（`tools/generate-sprites.mjs`、85枚）を拡張した**――外部生成画像を混ぜると12章が挙げる理由3つ（再生成できる・差分がレビューできる・単一HTMLに data URI で同梱できる）が同時に崩れる。`TILE_SIZE = 32` は不変。バリエーションは `GameState` に保存せず**タイル座標から導出**する（森の既存方式の一般化）。ただし既存の `(x * 7 + y * 13) % 2` を `% 3` へ広げると **`(x + y) % 3` に潰れて斜めの縞が出る**（7 も 13 も 3 で割った余りが1）ので、`variantAt`（`src/render/tileVariant.ts` 新規）に切り出して MurmurHash3 の finalizer で混ぜる形にした。テストは対角線ごとの偏りを測り、**素朴な式を渡すと 1.0 になる**ことを別テストで確かめている――指標が縞を捕まえると示さなければ「縞が無い」の主張が空になる。農地は `!sown` と `growth <= 0.35` の両方が `farm0` を返していた（＝種まき前の空き地に既に芽が描いてあった）ので、芽の無い `farm_tilled` を足し、畝を4pxピッチの明暗帯に強めた。既存4枚の地形スプライトはバイト単位で不変 |
| 雲の影（#15） | 済 | 純関数 `cloudsAt`（`src/render/clouds.ts` 新規）と、それをスプライトにする `syncClouds`（renderer）に分ける —— `daylight.ts` / `syncNight` と同じ分割で、決められる部分はすべてヘッドレスでテストできる側に置く。雲は36個の固定表（位置・速度・半径5〜26タイル・濃さ）で全部が1つの風向きへ流れ、乱数は使わない。**タイル数に依らず一定コスト**（スプライト36枚、テクスチャは3種の複合シルエットを雲ごとに index 固定で使い回す）。端の回り込みは地図の外へ余白（`WRAP_MARGIN_TILES = 28`、最大半径を下回らない値）を取ってその帯で alpha を0へ落としてから折り返す――座標が飛ぶ瞬間には必ず影が消えているので見た目の瞬間移動が起きない（テストで固定）。`state.speed === 0` の間は経過時間を進めない。レイヤは `colonistLayer` と `nightLayer` の間（影は建物も動物も暗くするが、夜のティントの下）。影のテクスチャは真円ではなく複合シルエット3種（大きな塊1つへ小さな塊を偏らせて重ねた形）を明るい青 `rgba(36, 84, 196)` のグラデーションで描き、`blendMode: 'multiply'` で合成する（アルファ合成は覆った画素を地面のテクスチャごと1色へ潰すため、乗算へ変えて地面の見た目を残した——テクスチャの色と blendMode は必ず一緒に変える）。`CLOUD_ALPHA_MAX = 0.28`（`clouds.test.ts` の「0.3未満」制約は変えていない）。**風（草木の揺れ）は対象外**――スプライトのアニメフレーム追加になり、design-phase7-time.md 5章が非目標に挙げている性質のもの |
| 選択パネルの分割（#12） | 済 | タイルは盤面左下 `.overlay--bl`、入植者・動物は右下 `.overlay--br`。issue の前提（右サイドバーにある）は古く、枠はフェーズ13段階Bで既に左下へ移っていたので、残っていたのは**1枠を2枠に割ること**だけだった。割った理由は `selectedTileId` が colonist / animal と**排他ではない**こと――`handleSelectClick` は毎クリック `selectTile` も呼ぶので両方が同時に立ち、1枠に積むと目立つ地面に立っている入植者のシートが押し出されていた。右下はミニマップ（`.overlay--tr`）の真下なので `max-height: calc(50% - 16px)` で頭打ちにする。`SelectionFrame` は生物用の箱として残す（この2つは store 上で排他のままなので、統合した理由はそちらには依然として効く） |

**雲の影（#15）実測（画面に影がある割合、視界サイズの窓を多数の位置に置いたサンプリング）**: 120×120・20分ぶんの経過時間で、5個＝14.3% → 26個＝82.4% → 36個＝**90.3%**（いずれも現行の `WRAP_MARGIN_TILES = 28` で測り直した値。個数を増やす過程の中間値・`WRAP_MARGIN_TILES` ごとの再測定は design-notes.md）。

**雲の影（#15）で目視して見つかったこと**: フェーズ6で地図が60×60→120×120へ4倍になったとき、雲の個数（5個）の表が追随しなかったのが根本原因——風（#23）で見つかったのと同じ「密度不足」だった。個数を増やしても薄すぎたため、濃さ（`CLOUD_ALPHA_MAX` 0.18→0.28）・大きさ（半径5〜10→5〜26）・色（暗い紺→乗算前提の明るい青）・合成方法（アルファ→乗算）・形（真円→複合シルエット3種）の順に見直した。途中、複合シルエットの初版が塊の半径をテクスチャ中央（正規化座標で最大0.28）に収めてしまい、`syncClouds` の拡大規則（256px = radius×2タイル）のもとで指定した半径の半分以下にしか広がらず画面から消える不具合を自分で作って直している——指定と実物が食い違う「プレイヤーに見えない失敗」の一種（経緯は design-notes.md「雲の影の追加調整（#15 の積み残し）」）。

### プレイテスト由来の調整1件（#10）

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 野生動物の頭数を半減（#10） | 済 | `SPECIES` の捕食者以外7種（deer / boar / rabbit / chicken / goat / crystalElk / rockeater）の `initialCount` を約半分に（60×60基準の合計 38→20）。初期配置と毎日の補充が同じ1つの値を読む設計（`worldgen.ts` / `animals.ts`）なので、変更点はこの1箇所。狼の上限（`scenario.predators`）は据え置き。実測: 個体数は狙いどおり約52.6%に、狩猟由来の食料は1年で7%減にとどまる（狩猟の律速は個体数ではなく狩人の処理能力だった）。**「獲物が減って狼が入植者に向かう」は起きなかった** —— 3シードでは入植者死亡が1→3に見えたが、12シードに広げると 3→4 で差は消えた。狼の頭数は獲物の量に連動せず（上限は `scenario.predators`）、1頭あたりの捕食速度も獲物の量に依存しないので、獲物不足が実際に起きる密度まではまだ下げていない（design-notes.md「野生動物の密度（#10）」） |

### プレイテスト由来の追加1件（#23）

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 風が渡る明度の帯 | 済 | issue が挙げた3案（(a) スプライトへのアニメフレーム追加・(b) 描画時の変形・(c) 明度の帯を流す）のうち**(c) を採用**、(a)・(b) は不採用。雲の影（`src/render/clouds.ts`）が既に「動く帯を1枚重ねる」機構を持ち、風向きと速度を与えるだけで済むため——CLAUDE.md「新しい機構より、既存の数値を曲げる」に沿う判断。実装は純関数 `windAt`（`src/render/wind.ts`）+ `wind.test.ts` + `renderer.ts` の `syncWind`。`GUSTS` は10個の固定表（26〜36×7〜9タイル・10.8〜15.6タイル/秒）で**乱数は使わない**——各ガストの位相は `tileVariant.ts` の `variantAt` をガストの座標（`x0`, `y0`）に当てて導出する。各ガストのテクスチャは長い筋1本＋短く淡い筋1本（計2本）で、方向そのものを形に持たせている（放射グラデーションの「もや」には方向が入らず、動く光の塊にしか見えなかった経緯は design-notes.md）。`WIND_STRENGTH_MAX = 0.18`。雲の影の `CLOUD_ALPHA_MAX` と直接比較する当初の主張（風は雲の半分以下）は撤回した——雲は暗いテクスチャを重ね風は白を加算するので同じ数値が同じ明るさを意味せず、代わりに両者がそれぞれ0.5未満（光源ではなく波紋のままであること）をテストで固定している。**タイル数に依らない一定コスト**（スプライト10枚、雲の影と同じ性質）。`GameState` に増えたフィールドはゼロ、`schemaVersion` は不変。ポーズ中（`state.speed === 0`）は経過時間が進まず止まる。レイヤーは terrain の直後・building の手前（雲の影よりさらに下） |

**実測**: 120×120 の実ビルドで 119 フレーム連続測定し、平均 16.67ms・中央値 16.70ms・p95 16.80ms・最大 16.90ms。60fps に張り付いており、風を足したことによるフレーム落ちは無い（vsync 上限なので余裕の量までは測れていない）。

**目視で見つかったこと**: 3回の目視調整を経て現在の形になった。(1) 初版は**一度も画面に現れなかった**——120×120 に 18×4 タイルの帯が4本、4.8〜6.5 タイル/秒では視界（約16タイル）を横切るのが約3秒で残りの時間はどこにも無く、明度上限 0.08 もそもそも見えない濃さだった。巨大な静止ガストを一時的に置いて確かめ、描画機構は正しくパラメータが原因と切り分けた。(2) 10本・26〜36タイル・1.8〜2.6タイル/秒・上限0.22へ直した後も「光のもや・霧が移動しているように見える」——放射グラデーションを楕円に引き伸ばしたテクスチャには方向が入っておらず、速度や配置をどう変えても移動する光の塊にしか見えなかった。6本の細い筋を描くテクスチャへ差し替えた。(3) 6本は「風」ではなく網掛けの帯に見えたため2本へ絞り、速度をさらに10.8〜15.6タイル/秒へ上げ（1.8〜2.6は漂流、5.4〜7.8でもまだ遅いという中間判断を経ている）、上限を0.18へ下げた。経緯は design-notes.md。**この「テストは緑でも画面には無かった」失敗パターンは雲の影（#15）でも再現しており、教訓として GitHub issue #30 に起票済み**（対応は #30 に委ねる）。

### フェーズ15: 音の層（重要度・BGM・音声ファイルの同梱） — 実装済み

当初のロードマップに無い追加。設計案は [design-phase15-audio.md](design-phase15-audio.md)。発端は実プレイからの要望（issue #17）で、
段階S-1（重要度の層・鳴らす場所・音量スライダー）と段階S-2の仕組み（ファイルがあれば差し替える機構）が先に実装され、
その時点では音声ファイルもBGMも無かった。今回 issue #22 で段階S-2の**実データ**（効果音・BGM本体そのもの）と
段階S-3（BGM再生層）が揃い、フェーズ15は当初の設計案が挙げた段階をすべて終えた。

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 重要度の層と鳴らす場所（段階S-1） | 済 | alarm/event/ambient の3層とゲイン・最短間隔の表、`LOG_SFX` を全 `LogKey` 網羅の `Record<LogKey, SfxName \| null>` にした。詳細は design-phase15-audio.md 実装メモ |
| 音声ファイルの差し替え機構（段階S-2・仕組み） | 済 | `src/assets/audio/<名前>.<拡張子>` を `import.meta.glob` で拾い、無ければ合成音にフォールバックする。1本も無くてもゲームは鳴る。詳細は同上 |
| 効果音・BGM本体の生成（段階S-2・実データ、#22） | 済 | `tools/generate-audio.ps1` が19音（効果音13種・うち `build`・`animal` は3バリエーション・BGM2本）の**プロンプトと seed と長さの表**を保持する——音はスプライトと違い決定論的に再現できないので、リポジトリが保存するのは「発注内容」まで。生成は手元の Stable Audio 3 Medium 環境（`C:\Dev\StableAudio3`、RTX 5070 12GB）で行い、44.1kHz stereo の WAV を得た。`tools/process-audio.mjs` が後処理（モノラル化 → 先頭無音の除去 → 目標長へ切り詰め＋末尾10msフェード → ピーク -3 dBFS へ正規化 → 16bit PCM 書き出し）を行って `src/assets/audio/` へ書き出す。依存パッケージの追加はゼロ。**BGM だけは切り詰めない**（`TARGET_DURATIONS` が `Infinity`）——ループする音に末尾フェードを掛けると1周ごとの音量の落ち込みそのものが継ぎ目になるため |
| BGM 再生層（段階S-3、#22） | 済 | `src/ui/bgm.ts`（純粋な決定層。ゲインを `daylight.ts` の `shadeAt` から導出し、昼夜の混ぜ比率が画面のティントと食い違わないようにする）と `src/ui/bgmPlayer.ts`（再生層）に分離。継ぎ目対策は「毎回 `start()` し直さない」こと——`loop = true` と `loopStart`/`loopEnd` をバッファ全体に張り、ソース生成と `start()` は生涯1回、以降は `gain.gain.value` しか動かさない。音量とミュートは効果音と**完全に独立**（`localStorage` キーも別、`BGM_STORAGE_KEY`）。既定はミュート。ポーズ中（`state.speed === 0`）は止まる |
| 合成音のフォールバック | 維持 | 音声ファイルが1本も無くても合成音で鳴る性質（issue #17由来）はそのまま |
| 配布先（GitHub Pages の追加、#22） | 済 | 単一HTML（`dist/simworld-embed.html`）が音声ファイルの追加により16,339,023バイト（15.58 MiB）となり、Claude アーティファクトの上限16MBに対して境界上になったため、音質を落とさず `.github/workflows/pages.yml` を追加した。通常ビルドの `assetsInlineLimit`（200000）は変えていないので BGM は別ファイルとして出力され、実測 **12.8 MB**（GitHub Pages のサイト上限1GBに対して十分）。`base: './'` は既存のままで `vite.config.ts` への変更は不要だった。**単一HTMLのビルドは置き換えではなく維持**、Pages は追加の配布経路 |

**未実施の検証**: 効果音とBGMの試聴。長さ・ピーク値・非無音・形式といった機械的な条件は満たしているが、
「ゲームに合う音か」「ループの継ぎ目が聞こえないか」は人間の試聴が要る。まだ試聴していない。

### プレイテスト由来の修正（#26・#27・#30）

| 項目 | 状態 | 内容 |
| --- | --- | --- |
| 狭い幅で盤面が潰れる（#26） | 済 | 固定幅のサイドバー（左260＋右300＝560px）と、`flex-wrap: wrap` で2〜4行に折り返す `.topbar` が、盤面を細い帯にしていた。**盤面の面積**を最優先の制約に置き、`src/ui/layout.ts`（新規）の純関数 `layoutFor(幅, 高さ)` が「どちらのサイドバーを常設するか」を決める —— 過半を保てなくなったら**右（読むパネル群）から先に**外し、それでも足りなければ左（建築ツールバー）も外す。外れたサイドバーは消えるのではなく**盤面に重なるドロワー**になる（閉じている間は盤面の幅を1pxも取らない）。ツールバーは `flex-wrap: nowrap` にし、幅が足りないときは操作ボタン群を `<details>` の「…」メニューへ畳む（新しい状態管理は増やさない）。開閉は既存の `localStorage`（`panelState.ts`）で、**`GameState` には1フィールドも増えない**。実ブラウザ実測（Chromium、`.app__viewport` の面積比）: **1024→40.1%が70.3%、800→23.3%が62.5%、640→9.0%が90.8%**、ツールバーは3〜4行が全幅で1行（44px）に。1600 / 1920 では両方常設のまま |
| 速度ボタンの表記（#27） | 済 | 表記が `⏸ ▶ ▶▶▶ ▶▶▶▶` で2番目のグリフ（▶▶）が飛んでおり、グリフ数が 1・3・4 と段階に対応していなかった。原因は表記ではなく**同じ事実が2か所にあったこと**——`TopBar.tsx` が自前の `speeds` 配列を、`useKeyboardShortcuts.ts` が別に `{ '1': 0, '2': 1, '3': 3, '4': 10 }` を持ち、互いにずれる自由があった。`src/ui/speedSteps.ts` に4段階（0/1/3/10）の表を1つ置き、両者がそこから引く。キーボードの 1/2/3/4 は**配列の添字そのもの**なので構造としてずれない。表記は倍率そのもの（en `⏸ / 1x / 3x / 10x`、ja `⏸ / 1倍 / 3倍 / 10倍`）で、`strings.ts` の辞書キー経由・型に載せてあるので訳し漏れはコンパイルで落ちる。倍率の実体は `src/game/loop.ts` の `speed`（tick 境界ごとに `ticks += speed`、tick 長 200ms 固定）——issue が挙げる `paceMultiplierOf`（`src/core/pace.ts`）は入植者の歩速でゲーム速度ではない |
| 描画層の画面占有率（#30） | 済 | 「実装され、テストも全部緑で、**一度も画面に出ていない**」を捕まえる指標が無かった。`src/render/screenCoverage.ts`（新規）が、時間変化する円の集合を視界窓（16×14タイル）で走査して占有率を返す**純関数**。**雲にも風にも依存しない**ので、将来の環境演出にもそのまま使える（風の帯は「幅/2 を半径とする円を長軸に沿って並べたカプセル」として渡す）。雲と風の枚数を**地図の面積から導く**ようにした（基準面積 = 120×120。`round(表の長さ × 面積 / 基準面積)`）——**基準面積ちょうどでは表をそのまま返す分岐**に入るので、`frontier`（120×120）の出力はこの変更の前後で1ビットも変わらない（テストで固定）。表より少ない枚数のときは**等間隔に間引く**（先頭N個ではない——表は小→大→巨大な塊→埋めの順なので、先頭を取ると大きい雲が全部消える）。多いときは表を巡回し、周回ごとに決定論的なオフセットを足す。実測は下段。テストは寸法を直書きせず **`src/core/constants.ts` の `MAP_SIZES` から駆動する**ので、出荷寸法が足された瞬間に自動で測られる——issue の「将来また地図サイズが変わったとき気づかせる」をコメントではなく構造で満たしている。指標が問題を捕まえることは、**修正前のパラメータ（雲5個・風4本）を通すと下限を割る**テストで別に示している（`tileVariant.test.ts` と同じ形）。これらのテストは当時の `WRAP_MARGIN_TILES` をリテラルで持って**自己完結**しており、現在の定数を import しない——測定記録は後の定数変更から独立している必要がある。合成の上限も固定した。ただし**雲は乗算合成**（`blendMode: 'multiply'`、色 `rgba(36, 84, 196)`）なので「`1 - Π(1 - alphaᵢ)`」というアルファ合成のモデルは誤り。channel ごとの透過率 `1 - (1 - c/255)·a` を Rec.709 の輝度重みで畳むと**1層あたり `1 - 0.679a`** で、この係数はコード内で色から計算している（マジックナンバーにしない）。実測の最悪値は 120×120 で **0.319**、`NIGHT_ALPHA`（0.45）を下回る |
| 真夜中スタート（#25） | 済 | 新規ワールドが `tick: 0` から始まり `hourOf(0)` は 00:00 —— プレイヤーが最初に見る画面が夜のティントで、直近フェーズの見た目の投資（地形バリエーション・雲の影・風・農地の生育段階）が第一印象で1つも見えなかった。`START_HOUR = 7` / `START_TICK = 875` を置き、`createEmptyState` の `tick` をそこから始める。**表示上のオフセットではなく実 tick**（issue が明示的に否定している案を採らない）——`tick` を読む系すべてが同じ時計を見るので、「導出した時刻と実体が食い違う」場所が1つも生まれない。`schemaVersion` は不変。`tick % INTERVAL === 0` を使う系が20か所ほどあり（`arrivals` / `regrowth` / `raid` / `events` ほか）位相は全部ずれるが、頻度は変わらない。12シード×1年の実測: 食料 1281.5→1189.5、人口 5.33→5.25、襲撃 1.00→1.08、死者 0.67→0.58。食料の −7.2% が最大だが**対応のある t 検定で t = −1.41（n=12、p ≈ 0.19）で有意でない** ——シードごとの差が +352〜−489 と両方向に振れており、系統的な有利不利ではなく別の軌跡をたどった結果。ただし n=12 では15%未満の系統差は検出できない |
| 年代記（#28） | 済 | 物語の部品（特性・気分・メンタルブレイク・関係性・死・襲撃・移住・手懐け）は既にそろっていて、足りないのは**出口**だった。`state.log` は100件のリングバッファなので、生まれた物語はプレイヤーが読み返す前に消える（CLAUDE.md 自身が「長時間の実行結果をログから測るな」と注意しているとおり、あれは記録ではなく直近の通知）。`src/core/chronicle.ts`（新規）に**少数精鋭の年代記**を持つ —— 対象は7種（死・襲撃の発生と結果・移住・手懐け・メンタルブレイク・季節/年の変わり目・研究完了）だけで、**全ログを保存するのではない**（それはリングバッファを大きくするだけでノイズごと残る）。`GameState` に持つ —— 過去の出来事は現在の状態から導出できないので「導出できるものは保存しない」とは矛盾しない側。**文言は保存せず辞書キー＋パラメータ＋tick**（`state.log` と同じ形）—— 保存された日本語文はセーブが言語に依存してしまう（フェーズ9）。`schemaVersion` 27→28、移行1段（古いセーブは空の年代記で開く）。上限は `CHRONICLE_MAX = 500`。**単純なリングバッファにはしていない** —— 古い順に捨てると「後から語り直せる記録」の目的そのもの（初期の物語）が真っ先に消えるため。最古30件は常に保護し、それ以降は**季節の巡り・メンタルブレイクという性質上くり返すものから先に間引く**。間引ける日常が尽きたら保護区間の直後を落として記録を止めない。実測（10シード×1年・60×60・無操作）: 合計92件＝**9.2件/年**（最悪シード17件/年）。内訳は `seasonArrived` 4.0・`colonistArrived` 2.9・ブレイク1.2・襲撃関係0.9。`researchUnlocked` と `animalTamed` は**プレイヤー操作が要るので無操作runでは0件** —— 実プレイではもっと多いので、50件/年を大盤振る舞いの天井として10年＝500件とした。1エントリ120バイト未満、500件でも60KB未満。読む場所は右サイドバーの `Fold`（`ChroniclePanel.tsx`、`EventLog` の隣・既定は閉じ）。エントリの文言は `EventLog` と**同じ辞書経由**（`strings.log[key](params)`）で、変換の仕組みを二重に作っていない。日付は `TopBar` と同じ `seasonDay` 形式で出し、年が変わる位置に見出しを挟む。`beginTick` は `chronicle` を参照ごと持ち回るので、`EventLog` と同じ `useShallow` ＋ `slice`/`reverse` の流儀に揃えて**毎tick再描画を避けている**。表示は150件で頭打ち |

## 12. ドット絵アセット仕様
Codexへドット絵生成を依頼する際にそのまま渡せる仕様。**MVPに必要なものだけ**を挙げる（フェーズ2以降の魔力インフラ系スプライトは対象外）。
### タイルサイズ
- **32×32px**（実装済み。`TILE = 32` / `TILE_SIZE = 32`）。理由: RimWorld系のキャラクター・小物の視認性と、PixiJSのテクスチャアトラス／スプライトバッチ効率のバランスが良い解像度。1タイル=1テクスチャの単純な対応にできる。
- キャラクター（入植者）も32×32のセルに収める（はみ出す装飾は将来検討、MVPでは不要）。
### パレット方針
- 各スプライトにつき**32色以内**の制限パレットを使う。理由: ファンタジー世界観の統一感を出しつつ、後で色数を絞ったパレット差し替え（染色・季節変化等）をしやすくするため。
- 地形は明度差で判別しやすい配色にする（草地=中明度の緑、森=低明度の濃緑、岩=中〜低明度のグレー、水=青系で浅瀬は明るく深水は暗く）。草地3種・森3種・岩2種のバリエーションはタイル座標から導出する（`variantAt`）。理由: 60×60の見下ろし視点でとっさに地形を識別できる必要がある。
- 建築物は地形より明るい/彩度の高い配色にして、地形の上に乗っていることが一目でわかるようにする。
- 資源アイコン（木材・石材・食料）は原色に近い高彩度色を使い、UI上の一覧表示で区別しやすくする。
- 出力形式: 透過PNG、1px黒〜濃色のアウトラインを入れる（背景タイルに溶け込ませない）。
### 必要スプライト一覧
| 分類 | 名前 | サイズ | 枚数/フレーム数 | 備考 |
| --- | --- | --- | --- | --- |
| 地形 | 草地タイル | 32×32 | 1 | バリエーション不要（MVPは単調柄でよい） |
| 地形 | 森タイル | 32×32 | 1〜2 | 木がまばらな見た目にするなら2枚 |
| 地形 | 岩タイル | 32×32 | 1 | 採掘前の状態 |
| 建築 | 壁 | 32×32 | 1（完成）+1（建築中ブループリント） | ブループリントは半透明表現 |
| 建築 | 床 | 32×32 | 1 | |
| 建築 | 扉 | 32×32 | 2（開/閉） | |
| 建築 | ベッド | 32×32 | 1 | |
| 建築 | 畑（畝） | 32×32 | 2〜3（種まき直後/成長中/収穫可） | 農作ジョブの進捗表現に使う |
| 建築 | 貯蔵ゾーンマーカー | 32×32 | 1 | 床タイルに重ねる枠線程度の表現でよい |
| 資源アイコン | 木材 | 16×16 or 32×32 | 1 | UI（資源一覧）とマップ上のドロップアイテム兼用 |
| 資源アイコン | 石材 | 16×16 or 32×32 | 1 | 同上 |
| 資源アイコン | 食料 | 16×16 or 32×32 | 1 | 同上 |
| 入植者 | 歩行アニメーション | 32×32 | 4方向×2〜4フレーム | MVPは3人だが個体差は色替えで対応し、共通の歩行スプライトシートを使い回す想定 |
| 入植者 | 作業アニメーション | 32×32 | 1〜2フレーム（簡易） | 伐採/採掘/農作/建築で使い回す簡易モーションでよい |
| UIアイコン | 仕事アイコン5種 | 16×16 or 24×24 | 各1 | 伐採・採掘・農作・建築・運搬（仕事優先度表で使用） |
| UIアイコン | ニーズアイコン2種 | 16×16 or 24×24 | 各1 | 空腹・睡眠（入植者タブで使用） |
木・岩そのもの（伐採前の木、採掘前の鉱脈）は森タイル・岩タイルの絵柄に含める設計とし、個別オブジェクトスプライトとしては起こさない。これによりMVPで必要な新規スプライト枚数を最小限に抑える。

### 現在の枚数

`tools/generate-sprites.mjs` が **61枚**を決定論的に生成する（`npm run sprites`）。当初一覧に対して増えたのは、地形（マナ結晶）、建築（石壁・石床・ベリー2種・霜花2種・牧場マーカー・魔導炉・導管2種・魔力灯2種・自動採掘機2種・炉端・タレット2種・交易柱）、動物8種の歩行シート、襲撃者と商人、UIアイコン（狩猟・世話・体力・気分）。

外部の画像素材は使わず、すべてコードから描いている。理由: 決定論的に再生成でき、差分がレビューでき、単一HTMLに data URI で同梱できる。

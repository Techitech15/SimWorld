# SimWorld 次の検討

[design.md](design.md) は**仕様**（いま何がどう動くか）、[design-notes.md](design-notes.md) は**理由**
（なぜそうしたか・何を測ったか）を書く場所で、このファイルは**まだ実装していないもの**を書く場所。

分けている理由は他の2つと同じで、寿命が違うから。仕様は実装が変われば書き換わり、理由は実装が変わっても
残る。**検討は実装されたら消える** —— 項目が実装された時点で、現況は design.md の 11 章へ、
決め手になった理由と実測値は design-notes.md へ移し、ここからは削除する。ここに残っているものは
すべて「まだ着手していない」ことを意味する。

| | |
| --- | --- |
| [design.md](design.md) | 仕様。**何がどこまで進んでいるかの表は 11 章**にある（ここには置かない） |
| [design-notes.md](design-notes.md) | 実装したものの理由と実測値 |
| [design-phase2.5-animals.md](design-phase2.5-animals.md) | 生物レイヤーの設計案（提案時のまま） |
| [design-phase5-trade.md](design-phase5-trade.md) | 交易とファンタジー層の設計案（未着手。行商・幻想生物・幻想植生） |
| [design-phase6-space.md](design-phase6-space.md) | マップ拡張と画面の再配置の設計案（未着手） |
| **このファイル** | 未実装のものの検討。実装されたら上の2つへ移して消す |

数値は測ったものには測った条件（シード数・日数）を添える。測っていない見積もりは**見積もりと明記する**。

## 目次

1. 段階Bを読んで見つかったこと
2. 提案1: 段階Cの前に燃料の持続性を決める
3. 提案2: マナを目標とアラートに出す
4. 提案3: メンタルブレイクの複数化
5. 提案4: 作業台と調理
6. 提案5: 入植者どうしの関係
7. 提案6: 襲撃と防衛
8. 推奨する順序
9. 提案しないものとその理由

---

## 1. 段階Bを読んで見つかったこと

フェーズ2は段階Bまで実装され、結晶 → 魔導炉 → 導管 → 魔力灯が繋がった。次は段階C（自動採掘）だが、
その前に**確認しておかないと後戻りが効かなくなる**ことが1つある。

### 1.1 鉱脈の分布を測り直した

design-notes.md は鉱脈を「8世界を実測すると1世界あたり 4〜57 本」と記録している。段階Bで結晶が
**燃料**になった以上この分布は供給量そのものなので、200 シードで数え直した
（`generateWorld({ seed })` の `crystal` タイル数、seed 1〜200）。

| | 本数 |
| --- | --- |
| 最小 | **0** |
| 10 パーセンタイル | 9 |
| 中央値 | 40 |
| 平均 | 51.0 |
| 90 パーセンタイル | 110 |
| 最大 | 194 |

**8世界の 4〜57 は間違いではないが、尾を見ていない。** 実際の分布ははるかに広く、そして
**200 世界のうち 3 つは鉱脈が1本も無い**。その世界ではフェーズ2に一度も入れない —— 魔導炉を
建てても点かないのではなく、点ける燃料が地図上のどこにも存在しない。

### 1.2 燃料は再生せず、尾の世界は1年もたない

`BURN_TICKS_PER_CRYSTAL = 1980`（1日 3,000 tick の 0.66）なので、魔導炉1基は結晶を
**1日あたり約 1.5 個**燃やす。1本 6 個（`CRYSTAL_PER_VEIN`）として、世界の全鉱脈を掘り切ったときに
炉1基が燃え続けられる日数は次のとおり（1年 = 20日）。

| 世界 | 鉱脈 | 結晶 | 炉1基が燃える日数 |
| --- | --- | --- | --- |
| 最小 | 0 | 0 | **0** |
| p10 | 9 | 54 | 35.6 |
| 中央値 | 40 | 240 | 158.4 |
| p90 | 110 | 660 | 435.6 |

| | 世界数 |
| --- | --- |
| 炉1基を**1年**（20日）維持できない | **11 / 200** |
| 炉1基を**5年**（100日）維持できない | **65 / 200** |

中央値の世界は炉1基で8年もつので、**平均的には問題が無い**。問題は分布の尾で、そこは
「マナが乏しい世界」ではなく「フェーズ2が期限付きの世界」になっている。

そしてマナは**この植民地で唯一、再生しない資源**である。森は戻り（`forestCapacity` まで）、草は生え直し、
ベリーは実り直し、野生動物は端から補充され、岩は地図の面積ぶんある。結晶だけが掘り切って終わる。

いまはこれで実害が小さい —— 段階Bの消費先は魔力灯だけで、効果は気分 +5 である。灯が消えても
植民地は死なない。**段階C（自動採掘）で実害になる**。生産チェーンがマナに依存した状態で燃料が尽きると、
プレイヤーには打つ手が無い（掘る対象がもう無いので、掘って解決できない）。

### 1.3 マナは目標にもアラートにも出ない

`ResourcePanel` はグリッド数・供給・需要を出し、供給不足のグリッドをアラート色で示す —— ここは
段階Bで手当てされている。手当てされていないのは残り2つ。

| 場所 | 現状 |
| --- | --- |
| `src/core/goals.ts` | マナの項目が**無い**（`mana` の文字列が1つも無い）。8項目のどれもフェーズ2に触れない |
| `src/core/alerts.ts` | 同上。落ちたグリッドも空の炉もアラートに出ないので、**現場へ飛ぶ導線が無い** |

家畜の餓死・建物の損傷・貯蔵の満杯にはどれもアラートがあり、クリックで現場へ飛べる。
グリッドが落ちることは design-notes.md が「見て分かる事実1つ」として全落ちを選んだ根拠そのものなので、
その1つがアラートに出ていないのは設計意図の取りこぼしに見える。

目標パネル側はもっと単純で、**一度も結晶を掘っていない植民地は、フェーズ2の存在を知らされない**。
目標パネルはもともと「仕組みは増えたのにゲーム内にその存在を伝えるものが無い」ために作られたもので、
段階Bはまさにその状態にある。

---

## 2. 提案1（最優先）: 段階Cの前に燃料の持続性を決める

**目的**: 1.2 を、段階Cを積む**前に**決着させる。順序が要点である —— 自動採掘がマナに依存してから
燃料モデルを変えると、既存のあらゆる植民地の成立条件が変わる。依存する前なら、変えても
「灯が消えるかどうか」しか動かない。

**選択肢**:

| 案 | 内容 | 効くもの | 副作用 |
| --- | --- | --- | --- |
| (a) 下限を保証する | 生成時に鉱脈が一定数を下回ったら、シードから決めた位置に足す | 0本世界（3/200）が消える | 長期の枯渇には効かない |
| (b) 再生させる | 岩の奥の結晶が、地図ごとの上限まで日次確率で戻る | 枯渇そのものが消える | マナが希少でなくなりうる |
| (c) 燃料をやめる | 消費するのは供給量だけにし、結晶は建設コストに寄せる | 枯渇が消える | 「供給し続けられるか」という段階Bの核が消える |

**(c) は採らない。** 段階Bが入れたのは「材料が足りるか」ではなく「供給し続けられるか」という第二の
制約で（design.md 11 章）、燃料をやめるとその制約が「建てられるか」に戻ってしまう。実装済みの
機能の核を後から抜くことになる。

**推奨は (a) + 上限つきの (b)。**

- **(a) は段階Cの有無にかかわらず要る。** 鉱脈ゼロの世界は、プレイヤーが何をしても
  フェーズ2に入れない。下限は控えめでよく、**8本（48 結晶 ≒ 炉32日ぶん）**を出発点として提案する
  —— 「フェーズ2を体験できる」ことは保証し、「フェーズ2で暮らせる」ことは保証しない量。
  移行経路は既にある（v9→v10 が世界シードから鉱脈を埋め込んでいるので、同じ仕組みで足せる）。
- **(b) は段階Cを積むなら要る。** 形は森の再生とまったく同じにする ——
  `GameState.crystalCapacity`（生成時の鉱脈数）を持ち、岩に囲まれたタイルが日次確率で `crystal` に
  戻り、容量で止まる。森の再生で既に学んだ「**不動点が無い規則は放置すると地図を作り変える**」
  （森 1033 → 2253、草原 1892 → 672）の対策がそのまま使えるので、新しい失敗の仕方を増やさない。
  速さは「掘るより遅い」に置く —— 再生が採掘に追いつくと、マナは希少でなくなり石と変わらなくなる。

**やらないこと**: 炉の燃費強化（数値を1つ足すだけで枯渇の**期限が延びるだけ**であり、期限付きであること
自体は変わらない）。

**追記**: 当初ここには「結晶の交易・購入（キャラバンは 9 章の非実装リスト）」も挙げていたが、
[design-phase5-trade.md](design-phase5-trade.md) で**商人が地図の端から来る形**なら土台を変えずに済むことが分かったので取り下げた。
ただし交易が入っても **(a) の下限保証は要る** —— 理由は design-phase5-trade.md の 9 章にある。

**受け入れ条件**:

| | 動いたと言える条件 |
| --- | --- |
| a-1 | 200 シードのどれにも到達可能な鉱脈が下限以上ある（1.1 と同じ数え方で回帰させる） |
| a-2 | 中央値・p90 の世界の鉱脈数が実質変わらない（下限は尾だけに効き、豊かな世界を変えない） |
| b-1 | 掘り尽くした地図で、1年放置すると鉱脈が戻り始める |
| b-2 | 5年放置しても `crystalCapacity` を超えない（森の再生の回帰と同じ形） |
| b-3 | 再生速度が採掘速度を下回る（掘るより速く戻るなら希少ではない） |
| 共通 | マナ設備を建てない植民地の1年計測が、導入前と一致する |

---

## 3. 提案2: マナを目標とアラートに出す

**目的**: 1.3 を閉じる。提案1より小さく、独立して入れられる。

**最小実装**:

- `goals.ts` に2項目。どちらも既存の8項目と同じく**現在の状態から導出**し、操作履歴は使わない。
  - 「マナ結晶を掘る」——結晶の在庫と、地図上に露出した鉱脈の有無から。ヒント `Mine > 岩の奥の結晶`
  - 「灯りを点す」——魔導炉があり、かつグリッドが落ちていないこと。ヒント `Build > Mana furnace`
- `alerts.ts` に1〜2項目。「グリッドが落ちている」「炉が空」。既存のアラートと同じくクリックで現場へ飛ぶ。

**受け入れ条件**: マナに触れたことのない植民地に「掘る」目標が出る／炉が点いたら消える／燃料が切れたら
未達に戻る（目標は状態から導かれるので戻るはず、というのが `goals.test.ts` が既に固定している性質）／
グリッドが落ちるとアラートが出て、クリックで炉へ飛ぶ。

**やらないこと**: 目標パネルの項目数を増やしすぎない。8 → 10 で止め、それ以上は畳み方の再検討とセットにする。

---

## 4. 提案3: メンタルブレイクの複数化

**目的**: design.md 11 章のフェーズ3が「部分」としている `MentalState` を、遷移機械にする。

現在は `brooding`（仕事の放棄）1状態しかないので、気分が 20 を切ったときに起きることは常に同じで、
12 種の思考と 12 種の特性を掛け合わせた入力の多様さが、**出力では1つに畳まれている**。

**最小実装**: `ColonistActivity` の union に2つ足すだけで、新しいフィールドもループも増えない。

| 状態 | 内容 | 既存のどれに載るか |
| --- | --- | --- |
| `bingeing`（やけ食い） | 貯蔵の食料を続けて食べる（3食ぶん） | 既存の `eating` を回数で回す |
| `wandering`（放心） | 遠いタイルへ勝手に歩き出す | 既存の move order を自分で出す |

選ばれ方は気分の低さと特性で重み付ける（悲観家はやけ食いに寄る等）。既存の `brooding` と同じく
**この状態は保存する** —— 一口の食事で復帰しては、プレイヤーが何が起きたか見ないまま終わるため。

**受け入れ条件**: 3種すべてが実際に起きる／どれも仕事を止める／終われば復帰する／気分 40 以上では
起きない／やけ食いは実際に貯蔵を減らす（言葉だけの状態ではない）。

**やらないこと**: 暴力を伴うブレイク（他人を殴る・建物を壊す）。敵対関係と戦闘はフェーズ4の領域。

---

## 5. 提案4: 作業台と調理

**目的**: design-phase2.5-animals.md 11 章が「作業台の概念が要る」として除外した二次加工の**入り口だけ**を作る。
肉・卵・乳・作物・ベリーはすべて同じ `food` に落ちるので、食べ物に関する選択がいま「量」しか無い。

**最小実装**: `workbench` 1種と `craft` ジョブ1種、レシピ1本。

- 調理: `food` 10 を消費し、`Item.variant: 'meal'` を持つ `food` 10 を作る
- 効果: 満腹回復が 70 → 95、加えて気分に「まともな食事」+6
- **`ResourceType` は増やさない** —— design-phase2.5-animals.md 2 章が予告した `variant`（描画とツールチップ
  だけが見る表示用タグ。シミュレーションのロジックは `type` しか見ない）の初仕事になる

仕事優先度表は 7 列 → 8 列。既存のジョブ・運搬・貯蔵チェーンは無改造で動く。

**受け入れ条件**: 生と調理済みが両方貯蔵に入る／調理済みを食べた入植者の満腹回復と気分が実際に違う／
作業台を撤去してもジョブと予約が取り残されない／`variant` を持たない既存アイテムがそのまま読める（移行不要）。

**やらないこと**: 皮革・製材・複数レシピ・レシピのキューと繰り返し指定。1レシピで機構が正しいことを
確かめてから増やす。

---

## 6. 提案5: 入植者どうしの関係

**目的**: design.md 11 章フェーズ3の `Relationship`（未）。現在 `Colonist` は他の入植者への参照を
一切持たず、誰が死んでも残りの気分は1点も動かない（`mood.ts` の思考はすべて「自分の状態」か「設備」）。

**最小実装**: `Colonist.opinions: Record<ColonistId, number>`（-100..100）。人口上限8なので最大28組。
関係を動かす出来事は3つに絞る: 近くで一緒に働いた（日1回まで、+微小）／同じ部屋で寝た（+微小）／
誰かが死んだ（生存者全員に大きく負）。使い道は気分の思考1〜2行。

悲しみは日数で薄れる必要があり、これは気分の「保存しない」原則の唯一の例外になるので、
`Colonist.griefUntilTick` 1フィールドで済ませる。

**懸念（正直に）**: 人口上限8だと関係の物語は薄い。この提案は**人口上限を上げる判断とセット**で検討すべきで、
単独で入れると「増えたが誰も気づかない数値」になりうる。優先度を提案3・4より下に置いたのはこのため。

**やらないこと**: 恋愛・家族・派閥。関係グラフに意味を持たせるのは、まず気分に効くことを確かめてから。

---

## 7. 提案6: 襲撃と防衛（フェーズ4）

段階Bでタレットの前提（供給網と消費モデル）は**半分揃った** —— `MANA_DRAW` のテーブルに1行足せば
魔力消費型の設備は成立する。揃っていないのは残り半分で、入植者は反撃機構を一切持たない
（`fleeing` しかなく、これは design-phase2.5-animals.md 5 章の意図的な設計）。反撃を入れると武器・命中・射線が
芋づるで要る。

加えて、狼が既に「外圧」の役を実際に果たしている（柵を齧り、家畜を殺し、入植者を追う）。
**提案1が決着し、段階Cが済むまで着手しない。**

---

## 8. 推奨する順序

```
提案1（燃料の持続性）──┬─ (a) 下限保証は単独で入れられる
                        └─ (b) 再生は段階Cと同時でよい
   ↓
提案2（目標とアラート）── 小さい。提案1と並行して入れられる
   ↓
段階C（自動採掘）── design.md 11 章。提案1の決着が前提
   ↓
提案3（メンタルブレイク）── 小さく、単独で完結する
   ↓
提案4（作業台と調理）── variant の初仕事。以降の二次加工の土台
   ↓
提案5（関係）── 人口上限の判断とセットで
   ↓
提案6（襲撃と防衛）── 段階Cの完了が前提
```

提案1が先頭なのは、ロードマップの順序だからではなく、**後戻りが効かなくなる期限があるのがこれだけ**
だから。他の提案はいつ着手しても同じコストで入るが、燃料モデルは段階Cが積まれた瞬間から
「変えると既存の植民地の成立条件が変わるもの」になる。

---

## 9. 提案しないものとその理由

| 案 | 理由 |
| --- | --- |
| 娯楽・快適・美しさのニーズ | 3本目のゲージは `needs.ts`・UI・気分・特性・セーブ移行の全経路に波及するのに、得るものは「もう1本減るバー」。気分には既に**思考**という枠があるので、同じ体験は建築から思考を生やす形で1/10の変更量で出せる |
| 気温シミュレーション | design.md 11 章が温度管理を「気温が無いので保留」としているとおり。タイルごとの温度場を持つと断熱・部屋判定・服が芋づるで要る。冬に効く設備が欲しいなら、既存の `CROP_GROWTH_BY_SEASON` を曲げる形のほうが安い |
| 医療・部位・病気 | design.md 9 章が明示的に除外し、design-phase2.5-animals.md 1 章が範囲を「`health` 1つ」に固定した。捕食者を入れるための逸脱をこれ以上広げない |
| 死体・屠殺台・皮革 | design-phase2.5-animals.md が「作業台の概念が要る」として除外したもの。提案4で作業台が入ってから改めて |
| 炉の燃費強化だけで枯渇に対処する | 数値を1つ緩めても**期限が延びるだけ**で、期限付きであること自体は変わらない（1.2） |
| 人口上限の引き上げ単独 | 上限8は移住規則（`ARRIVAL_FOOD_PER_COLONIST`）と1年計測の両方に噛んでいる。提案5と一緒に、計測をやり直す前提で判断する |
| 複数マップ・キャラバン・研究ツリー | design.md 9 章の非実装リストのまま。どれも「1枚の地図の上の最適化パズル」という土台を変える。**ただし交易のうち「商人が来て去る」形だけは土台を変えない**ので [design-phase5-trade.md](design-phase5-trade.md) で別に検討した（キャラバン＝こちらから出ていく形は引き続き提案しない） |

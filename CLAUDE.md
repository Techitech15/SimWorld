# SimWorld — 作業上の約束

このリポジトリで作業するときの決まりごと。**設計の中身はここに書かない** —— 仕様は
[docs/design.md](docs/design.md)、理由は [docs/design-notes.md](docs/design-notes.md) にある。

## 言語

| 対象 | 言語 |
| --- | --- |
| **コミットメッセージ** | **日本語** |
| **プルリクエストのタイトルと本文** | **日本語** |
| ドキュメント（`README.md` / `docs/*.md`） | 日本語 |
| コード・コメント・識別子・ログ/文言の**キー名** | **英語**（既存がそうなので揃える） |
| **UI 文言** | **辞書キー経由**（`src/ui/strings.ts` に en / ja の両方を持つ。直書きしない） |

コードのコメントは英語のまま書く。日本語と英語が同じファイルの中で混ざるのが最も読みにくい。
プレイヤーに見える文言を足すときは、コンポーネントに直書きせず辞書（`Strings`）にキーを足す
——訳し漏れは型で捕まる（フェーズ9、`docs/design-phase9-language.md`）。

## ドキュメントの分担

4種類あり、**寿命が違うから**分けてある。書き足す前に、どれに属する内容かを決める。

| ファイル | 役割 | いつ書き換わるか |
| --- | --- | --- |
| `docs/design.md` | **仕様**。いま何がどう動くか。全12章 | 実装が変わるたび |
| `docs/design-notes.md` | **理由と実測値**。なぜそうしたか・何を測ったか・案から何を変えたか | 追記のみ。**過去の測定を消さない** |
| `docs/design-next.md` | **未実装**のものの検討と優先順位 | 実装されたら該当項目を削除する |
| `docs/design-phase<番号>-<名前>.md` | フェーズ単位で足す層の設計案。**提案時のまま凍結** | 原則書き換えない（差分は末尾の実装メモに追記） |

- 何がどこまで進んでいるかの表は **`design.md` 11章** にある。他の場所に第二の進捗表を作らない。
- フェーズ層の設計案は `design-phase2.5-animals.md` / `design-phase5-trade.md` の形。
  **フェーズに属さない3つ**（`design.md` / `design-notes.md` / `design-next.md`）にはこの命名を使わない。
- 項目が実装されたら、現況は `design.md` 11章へ、決め手と実測値は `design-notes.md` へ移し、
  `design-next.md` からは**消す**。

## コマンド

```bash
npm install
npm run dev           # http://localhost:5173
npm test              # ヘッドレスのシミュレーションテスト（約4分）
npm run typecheck     # tsc のみ
npm run build         # 型チェック + 本番ビルド
npm run sprites       # ドット絵アセットの再生成（決定論的）
npm run build:single  # 単一HTML（dist/simworld.html）
```

## 守るべきこと

**主張する前に測る。** バランスも性能も「こうなるはず」ではなく、ヘッドレス実行か実ビルドで
測ってから書く。測った値と食い違ったら**直すのは主張の側**。測定には条件（シード数・日数・試行数）を
必ず添える。標本が小さいときは、小さいと書く。

**追加は段階に割る。** 段階ごとに「動いたと言える条件」を**先に**決め、それをテストにしてから実装する
（`design.md` 10章）。

**新しい機構より、既存の数値を曲げる。** 特性は既存の数値への乗数として実装され、マナ結晶は
既存の `mine` ジョブの対象拡張で済み、炉の燃料補給は既存の `haul` ジョブで済んだ。
`ResourceType` / `JobType` / `GameState` のフィールドを増やす前に、既存の何かで済まないか考える。

**`GameState` はプレーンデータのまま保つ。** クラス・`Date`・`Map`・`Set` を使わない。参照は ID のみ。
`JSON.stringify` だけでセーブが完結する状態を崩さない（`design.md` 4章・8章）。

**導出できるものは保存しない。** 気分・思考・マナネットワーク・領域ラベルはどれも導出値で、
`SimContext`（セーブ対象外）か毎回の計算にある。保存された導出値は、それが説明している対象と食い違いうる。

**セーブを壊さない。** 型に手を入れたら `schemaVersion` を上げ、`migrations` に1段足す
（`src/persistence/saveFile.ts`）。移行できない古いセーブは黙って読まず、拒否する。

**プレイヤーに見えない失敗を作らない。** 資源が黙って消える・仕事が黙って無効化される・
アラートに出ない、を疑う。実際にこの形のバグを複数回出している（`design-notes.md` の
「魔力ネットワーク」「テストヘルパー」の節）。

## テスト

`npm test` は各段階の受け入れ条件をヘッドレスで検証する（59ファイル・432テスト、約8分）。
テストの一覧と対応する条件は `README.md` にある。

- **テストは 60×60 の地図で回す。出荷される既定は 120×120**（11章 フェーズ6）。世界を作るときは
  `createHarness` か `testWorld`（`testUtils`）を通し、**`generateWorld` を直接呼ばない** ——
  直接呼ぶと既定寸法に付いていってしまい、拠点の位置が変わって地形の主張が別の場所を見る。
  出荷寸法を見るのは `longrun` / `chaos` / `roundtrip` の3つだけ
- `src/core/longrun.test.ts` は無操作で1年（20日 = 60,000 tick）回す。tick 予算・セーブ往復・
  ジョブ滞留の破綻をここで捕まえる。120×120 で1年を回すのもここ
- `src/core/chaos.test.ts` は無作為なプレイヤー操作を流し込み、**不変条件**だけを検証する
  （「植民地がうまくやっている」ではなく「状態が破綻していない」）
- `state.log` は100件のリングバッファなので、**長時間の実行結果をログから測らない**
  （バッファを測ることになる）。`recordLog`（`testUtils`）を使う

## Git

- 作業は指定されたブランチで行い、`main` へ直接 push しない
- `git push -u origin <branch>`。ネットワーク起因の失敗のみ、指数バックオフで最大4回まで再試行
- プルリクエストは**依頼されたときだけ**作る
